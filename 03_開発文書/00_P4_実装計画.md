---
title: "Claudian Bridge P4 実装計画"
type: implementation-plan
version: 1.0.0
status: ✅ 已批准
created: 2026-08-10
modified: 2026-08-10
project_id: POC_017_ClaudianBridge
phase: 3
tags:
  - 実装計画
  - P4
  - 拡張子フィルタ統合
  - F005
language: Japanese
applied_rules_version: 2.9.2
---

# 🏗️ Claudian Bridge P4 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 旧プラグイン `extension-whitelist` の拡張子フィルタ機能（CSS注入による左サイドバーのファイル一覧絞り込み + プリセット5種 + タグUI）を Claudian Bridge の `src/features/whitelist/` に純ロジック（CSS生成・プリセットデータ）と Obsidian DOM 注入に分離して移植し、設定スキーマ・データ移行・設定タブ・i18n を統合する。

**Architecture:** `src/features/whitelist/` に `buildWhitelistCss`（純関数）と `installWhitelistCss/removeWhitelistCss`（DOM注入ラッパー）を分離。`core/settings.ts` に `WhitelistSettings` スキーマ追加、`legacy/` に extension-whitelist 移行を実装。`main.ts` で onload/unload/ConfigStore.watch から注入制御。

**Tech Stack:** TypeScript 5.x / esbuild 0.24+ / vitest 2.x / Node.js 20+ / Obsidian API 1.7.2+。

## Global Constraints

- ソースリポジトリ: `D:\AI-Agent\ClaudianBridge\`（git・main ブランチ）
- ビルド: `npm run build` で `main.js` 生成、esbuild production
- テスト: `npm test` で vitest 全テスト
- デプロイ: `npm run deploy` で `main.js` / `manifest.json` / `styles.css` を `.obsidian/plugins/claudian-bridge/` へコピー
- CSS クラス接頭辞: `cb-whitelist-*`（旧 `ext-whitelist-*` は使わない）
- 純ロジック（CSS生成・プリセット）は TDD、DOM 注入は薄いラッパー（手動 UAT）
- 旧プラグインの `:has()` セレクタ方式を **そのまま** 採用（後方互換）
- 移行時は `_disabled__` プレフィックス付き旧フォルダからも読む（Task 4 で対応済み）
- P4 コードは各 Task ごとに 1 commit（個別 revert 可能）

---

### Task 1: WhitelistSettings スキーマ追加（TDD）

**Files:**
- Modify: `D:\AI-Agent\ClaudianBridge\src\core\settings.ts`
- Modify: `D:\AI-Agent\ClaudianBridge\tests\core\settings.test.ts`

**Interfaces:**
- Produces: `WhitelistSettings` / `DEFAULT_WHITELIST_SETTINGS` / `normalizeWhitelistSettings(raw)`

- [ ] **Step 1: 失敗テストを追加**

`tests/core/settings.test.ts` の describe 内に追加:

```typescript
  it('DEFAULT_CLAUDIAN_BRIDGE_SETTINGS.whitelist は全フィールドを持つ', () => {
    expect(DEFAULT_CLAUDIAN_BRIDGE_SETTINGS.whitelist).toMatchObject({
      enabled: true,
      extensions: expect.arrayContaining(['md', 'pdf']),
      alwaysShowFolders: true,
    });
  });

  it('normalize は whitelist の欠落キーをデフォルトで埋める', () => {
    const norm = normalizeClaudianBridgeSettings({ whitelist: { enabled: false } });
    expect(norm.whitelist.enabled).toBe(false);
    expect(norm.whitelist.extensions).toEqual(expect.arrayContaining(['md', 'canvas', 'pdf']));
    expect(norm.whitelist.alwaysShowFolders).toBe(true);
  });

  it('normalize は extensions 要素を正規化する（trim/lowercase/先頭ドット除去/空文字除去）', () => {
    const norm = normalizeClaudianBridgeSettings({ whitelist: { extensions: ['.MD', ' PDF ', '', 'JSON'] as unknown as string[] } });
    expect(norm.whitelist.extensions).toEqual(['md', 'pdf', 'json']);
  });

  it('validate は whitelist 型違反を返す（enabled が boolean でない）', () => {
    const bad = { ...DEFAULT_CLAUDIAN_BRIDGE_SETTINGS, whitelist: { enabled: 'yes' as unknown as boolean, extensions: [], alwaysShowFolders: true } };
    expect(validateClaudianBridgeSettings(bad)).toContain('whitelist.enabled');
  });
```

- [ ] **Step 2: 実行して失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/core/settings.test.ts
```

Expected: FAIL（`whitelist` が `{}` のため `enabled` が undefined）

- [ ] **Step 3: 実装**

`src/core/settings.ts` の冒頭（`OfficeSettings` の後）に追加:

```typescript
export interface WhitelistSettings {
  enabled: boolean;
  extensions: string[];
  alwaysShowFolders: boolean;
}

export const DEFAULT_WHITELIST_SETTINGS: WhitelistSettings = {
  enabled: true,
  extensions: ['md', 'canvas', 'pdf', 'png', 'doc', 'docx', 'xls', 'xlsx', 'ppt', 'pptx'],
  alwaysShowFolders: true,
};

export function normalizeWhitelistSettings(raw: unknown): WhitelistSettings {
  const r = (raw ?? {}) as Partial<WhitelistSettings>;
  return {
    enabled: r.enabled ?? DEFAULT_WHITELIST_SETTINGS.enabled,
    extensions: Array.isArray(r.extensions)
      ? r.extensions
          .filter((e): e is string => typeof e === 'string')
          .map((e) => e.trim().toLowerCase().replace(/^\./, ''))
          .filter((e) => e.length > 0)
      : [...DEFAULT_WHITELIST_SETTINGS.extensions],
    alwaysShowFolders: r.alwaysShowFolders ?? DEFAULT_WHITELIST_SETTINGS.alwaysShowFolders,
  };
}
```

`ClaudianBridgeSettings` の `whitelist` フィールドを `WhitelistSettings` に変更:

```typescript
  whitelist: WhitelistSettings;
```

`DEFAULT_CLAUDIAN_BRIDGE_SETTINGS` の `whitelist: {}` を `{ ...DEFAULT_WHITELIST_SETTINGS }` に変更:

```typescript
  whitelist: { ...DEFAULT_WHITELIST_SETTINGS },
```

`normalizeClaudianBridgeSettings` の `whitelist: {}` を `whitelist: normalizeWhitelistSettings(r.whitelist)` に変更:

```typescript
    whitelist: normalizeWhitelistSettings(r.whitelist),
```

`validateClaudianBridgeSettings` に whitelist チェックを追加（office チェックの後）:

```typescript
  if (typeof cfg.whitelist.enabled !== 'boolean') return 'whitelist.enabled は boolean である必要があります';
  if (!Array.isArray(cfg.whitelist.extensions)) return 'whitelist.extensions は配列である必要があります';
  if (typeof cfg.whitelist.alwaysShowFolders !== 'boolean') return 'whitelist.alwaysShowFolders は boolean である必要があります';
```

- [ ] **Step 4: PASS 確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/core/settings.test.ts
```

Expected: PASS（既存 6 + 新規 4 = 10 件）

- [ ] **Step 5: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/core/settings.ts tests/core/settings.test.ts && git commit -m "feat(core): WhitelistSettings スキーマ + normalize + validate (TDD)"
```

---

### Task 2: プリセット（TDD）

**Files:**
- Create: `D:\AI-Agent\ClaudianBridge\src\features\whitelist\presets.ts`
- Test: `D:\AI-Agent\ClaudianBridge\tests\features\whitelist\presets.test.ts`

**Interfaces:**
- Produces: `WhitelistPreset` / `WHITELIST_PRESETS`（5件）

- [ ] **Step 1: 失敗テスト**

`tests/features/whitelist/presets.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { WHITELIST_PRESETS } from '../../../src/features/whitelist/presets';

describe('WHITELIST_PRESETS', () => {
  it('5 件のプリセットを持つ', () => {
    expect(WHITELIST_PRESETS).toHaveLength(5);
  });
  it('各プリセットは name / extensions / desc を持つ', () => {
    for (const p of WHITELIST_PRESETS) {
      expect(typeof p.name).toBe('string');
      expect(Array.isArray(p.extensions)).toBe(true);
      expect(typeof p.desc).toBe('string');
    }
  });
  it('「📦 ALL」プリセットは * フラグを含む', () => {
    const all = WHITELIST_PRESETS.find((p) => p.name.includes('ALL'));
    expect(all).toBeDefined();
    expect(all?.extensions).toContain('*');
  });
  it('「📄 Obsidian標準」は md / canvas のみ', () => {
    const obs = WHITELIST_PRESETS.find((p) => p.name.includes('Obsidian標準'));
    expect(obs?.extensions).toEqual(['md', 'canvas']);
  });
});
```

- [ ] **Step 2: 失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/features/whitelist/presets.test.ts
```

Expected: FAIL（`Cannot find module`）

- [ ] **Step 3: 実装**

`src/features/whitelist/presets.ts`:

```typescript
export interface WhitelistPreset {
  name: string;
  extensions: string[];
  desc: string;
}

export const WHITELIST_PRESETS: ReadonlyArray<WhitelistPreset> = [
  { name: '📄 Obsidian標準', extensions: ['md', 'canvas'], desc: 'Markdown + Canvasのみ' },
  { name: '📊 Office', extensions: ['doc', 'docx', 'xls', 'xlsx', 'ppt', 'pptx'], desc: 'Word/Excel/PowerPoint' },
  { name: '🖼️ 画像', extensions: ['png', 'jpg', 'jpeg', 'gif', 'svg', 'webp', 'bmp'], desc: '一般的な画像形式' },
  { name: '💻 コード', extensions: ['js', 'ts', 'py', 'json', 'yaml', 'xml', 'css', 'html', 'sh'], desc: 'ソースコード類' },
  { name: '📦 ALL', extensions: ['*'], desc: 'すべて表示（フィルター解除）' },
];
```

- [ ] **Step 4: PASS 確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/features/whitelist/presets.test.ts
```

Expected: PASS（4 件）

- [ ] **Step 5: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/features/whitelist/presets.ts tests/features/whitelist/presets.test.ts && git commit -m "feat(whitelist): 5 件のプリセット定数 (TDD)"
```

---

### Task 3: CSS ビルダー（TDD）

**Files:**
- Create: `D:\AI-Agent\ClaudianBridge\src\features\whitelist\css-builder.ts`
- Test: `D:\AI-Agent\ClaudianBridge\tests\features\whitelist\css-builder.test.ts`

**Interfaces:**
- Produces: `buildWhitelistCss(extensions: string[], alwaysShowFolders: boolean): string | null`

- [ ] **Step 1: 失敗テスト**

`tests/features/whitelist/css-builder.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { buildWhitelistCss } from '../../../src/features/whitelist/css-builder';

describe('buildWhitelistCss', () => {
  it('空配列 → null（フィルター無効化）', () => {
    expect(buildWhitelistCss([], true)).toBeNull();
  });

  it('拡張子リストから :has() セレクタを構築する', () => {
    const css = buildWhitelistCss(['md', 'pdf'], false);
    expect(css).not.toBeNull();
    expect(css).toContain('[data-path$=".md" i]');
    expect(css).toContain('[data-path$=".pdf" i]');
    expect(css).toContain(':has(');
    expect(css).toContain('display: none !important');
  });

  it('alwaysShowFolders=true → .nav-folder ルールを含む', () => {
    const css = buildWhitelistCss(['md'], true);
    expect(css).toContain('.nav-folder { display: flex !important }');
  });

  it('alwaysShowFolders=false → .nav-folder ルールを含まない', () => {
    const css = buildWhitelistCss(['md'], false);
    expect(css).not.toContain('.nav-folder');
  });

  it('コメント・id を含む', () => {
    const css = buildWhitelistCss(['md'], false);
    expect(css).toContain('Claudian Bridge Whitelist');
  });
});
```

- [ ] **Step 2: 失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/features/whitelist/css-builder.test.ts
```

Expected: FAIL

- [ ] **Step 3: 実装**

`src/features/whitelist/css-builder.ts`:

```typescript
export function buildWhitelistCss(extensions: string[], alwaysShowFolders: boolean): string | null {
  if (extensions.length === 0) return null;

  const extSelectors = extensions
    .map((ext) => `      [data-path$=".${ext}" i]`)
    .join(',\n');

  const folderRule = alwaysShowFolders
    ? '\n/* folders always visible */\n.nav-folder { display: flex !important; }'
    : '';

  return `/* ── Claudian Bridge Whitelist ── */
.nav-files-container .nav-file:not(:has(
  > .nav-file-title:is(
${extSelectors}
  )
)) {
  display: none !important;
}${folderRule}`;
}
```

- [ ] **Step 4: PASS 確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/features/whitelist/css-builder.test.ts
```

Expected: PASS（5 件）

- [ ] **Step 5: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/features/whitelist/css-builder.ts tests/features/whitelist/css-builder.test.ts && git commit -m "feat(whitelist): buildWhitelistCss — :has() セレクタ生成 (TDD)"
```

---

### Task 4: DOM 注入ラッパー

**Files:**
- Create: `D:\AI-Agent\ClaudianBridge\src\features\whitelist\injector.ts`

**Interfaces:**
- Produces: `installWhitelistCss(css: string): void` / `removeWhitelistCss(): void`

- [ ] **Step 1: 実装**

`src/features/whitelist/injector.ts`:

```typescript
const STYLE_ID = 'cb-whitelist-css';

export function installWhitelistCss(css: string): void {
  removeWhitelistCss();
  const el = document.createElement('style');
  el.id = STYLE_ID;
  el.textContent = css;
  document.head.appendChild(el);
}

export function removeWhitelistCss(): void {
  const existing = document.getElementById(STYLE_ID);
  if (existing) existing.remove();
}
```

- [ ] **Step 2: 型チェック**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm run typecheck
```

Expected: エラーなし

- [ ] **Step 3: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/features/whitelist/injector.ts && git commit -m "feat(whitelist): installWhitelistCss / removeWhitelistCss DOM ラッパー"
```

---

### Task 5: extension-whitelist → whitelist 変換（TDD）

**Files:**
- Create: `D:\AI-Agent\ClaudianBridge\src\legacy\convert-ew.ts`
- Test: `D:\AI-Agent\ClaudianBridge\tests\legacy\convert-ew.test.ts`

**Interfaces:**
- Produces: `convertFromExtensionWhitelist(raw): Partial<ClaudianBridgeSettings> | null`

- [ ] **Step 1: 失敗テスト**

`tests/legacy/convert-ew.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { convertFromExtensionWhitelist } from '../../src/legacy/convert-ew';

describe('convertFromExtensionWhitelist', () => {
  it('null 入力 → null', () => {
    expect(convertFromExtensionWhitelist(null)).toBeNull();
  });
  it('期待される whitelist 形式を返す', () => {
    const raw = { enabled: false, extensions: ['md', 'JSON', '.pdf '], alwaysShowFolders: false };
    const result = convertFromExtensionWhitelist(raw);
    expect(result).toMatchObject({
      whitelist: { enabled: false, extensions: ['md', 'json', 'pdf'], alwaysShowFolders: false },
    });
  });
  it('型違反はデフォルトで埋める', () => {
    const result = convertFromExtensionWhitelist({ enabled: 'yes', extensions: 'invalid' });
    expect(result?.whitelist.enabled).toBe(true);
    expect(result?.whitelist.extensions).toEqual(expect.arrayContaining(['md', 'canvas', 'pdf']));
    expect(result?.whitelist.alwaysShowFolders).toBe(true);
  });
});
```

- [ ] **Step 2: 失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/legacy/convert-ew.test.ts
```

Expected: FAIL

- [ ] **Step 3: 実装**

`src/legacy/convert-ew.ts`:

```typescript
import type { ClaudianBridgeSettings } from '../core/settings';
import { DEFAULT_WHITELIST_SETTINGS } from '../core/settings';

function normalizeExtList(raw: unknown): string[] {
  if (!Array.isArray(raw)) return [...DEFAULT_WHITELIST_SETTINGS.extensions];
  return raw
    .filter((e): e is string => typeof e === 'string')
    .map((e) => e.trim().toLowerCase().replace(/^\./, ''))
    .filter((e) => e.length > 0);
}

export function convertFromExtensionWhitelist(raw: unknown): Partial<ClaudianBridgeSettings> | null {
  if (!raw || typeof raw !== 'object') return null;
  const r = raw as Record<string, unknown>;
  return {
    whitelist: {
      enabled: typeof r.enabled === 'boolean' ? r.enabled : DEFAULT_WHITELIST_SETTINGS.enabled,
      extensions: normalizeExtList(r.extensions),
      alwaysShowFolders: typeof r.alwaysShowFolders === 'boolean' ? r.alwaysShowFolders : DEFAULT_WHITELIST_SETTINGS.alwaysShowFolders,
    },
  };
}
```

- [ ] **Step 4: PASS 確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/legacy/convert-ew.test.ts
```

Expected: PASS（3 件）

- [ ] **Step 5: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/legacy/convert-ew.ts tests/legacy/convert-ew.test.ts && git commit -m "feat(legacy): convert from extension-whitelist data (TDD)"
```

---

### Task 6: migration に extension-whitelist 追加（TDD）

**Files:**
- Modify: `D:\AI-Agent\ClaudianBridge\src\legacy\migration.ts`
- Modify: `D:\AI-Agent\ClaudianBridge\tests\legacy\migration.test.ts`

**Interfaces:**
- Consumes: `convertFromExtensionWhitelist`（Task 5）
- Produces: `migrateFromLegacy` が `extension-whitelist` を `migrated` に含める

- [ ] **Step 1: 失敗テスト**

`tests/legacy/migration.test.ts` に追加:

```typescript
  it('_disabled__extension-whitelist の data.json を変換してフラグを立てる', () => {
    mkdirSync(join(dir, '_disabled__extension-whitelist'));
    writeFileSync(
      join(dir, '_disabled__extension-whitelist', 'data.json'),
      JSON.stringify({ enabled: false, extensions: ['md', 'JSON'], alwaysShowFolders: false })
    );
    const result = migrateFromLegacy(store, dir);
    expect(result.migrated).toContain('extension-whitelist');
    const loaded = store.load();
    expect(loaded.whitelist.enabled).toBe(false);
    expect(loaded.whitelist.extensions).toEqual(['md', 'json']);
    expect(loaded.whitelist.alwaysShowFolders).toBe(false);
    expect(loaded.general.migratedFrom.extensionWhitelist).toBe(true);
    expect(existsSync(join(dir, '_disabled__extension-whitelist', 'data.json.bak.json'))).toBe(true);
  });

  it('extension-whitelist は 2 度目の呼び出しでは移行しない', () => {
    mkdirSync(join(dir, '_disabled__extension-whitelist'));
    writeFileSync(join(dir, '_disabled__extension-whitelist', 'data.json'), JSON.stringify({ enabled: true, extensions: ['md'], alwaysShowFolders: true }));
    migrateFromLegacy(store, dir);
    const result2 = migrateFromLegacy(store, dir);
    expect(result2.migrated).not.toContain('extension-whitelist');
  });
```

- [ ] **Step 2: 失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/legacy/migration.test.ts
```

Expected: FAIL

- [ ] **Step 3: 実装**

`src/legacy/migration.ts` の import に追加:

```typescript
import { convertFromExtensionWhitelist } from './convert-ew';
```

`// extension-whitelist → P4 で実装` を置換:

```typescript
  // extension-whitelist → whitelist
  if (!cfg.general.migratedFrom.extensionWhitelist) {
    const raw = readLegacyDataJson(pluginsDir, 'extension-whitelist');
    if (raw) {
      const partial = convertFromExtensionWhitelist(raw);
      if (partial) {
        const merged = { ...store.load(), ...partial };
        store.save(merged);
        const bak = backupLegacyData(pluginsDir, 'extension-whitelist');
        if (bak) result.backup.push(bak);
        result.migrated.push('extension-whitelist');
        const cfg2 = {
          ...store.load(),
          general: { ...store.load().general, migratedFrom: { ...store.load().general.migratedFrom, extensionWhitelist: true } },
        };
        store.save(cfg2);
      }
    }
  }

  return result;
```

- [ ] **Step 4: PASS 確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/legacy/migration.test.ts
```

Expected: PASS（既存 6 + 新規 2 = 8 件）

- [ ] **Step 5: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/legacy/migration.ts tests/legacy/migration.test.ts && git commit -m "feat(legacy): migrateFromLegacy に extension-whitelist 追加 (TDD)"
```

---

### Task 7: SettingTabWhitelist 本実装 + i18n

**Files:**
- Modify: `D:\AI-Agent\ClaudianBridge\src\settings\SettingTabWhitelist.ts`
- Modify: `D:\AI-Agent\ClaudianBridge\src\core\i18n.ts`
- Modify: `D:\AI-Agent\ClaudianBridge\tests\core\i18n.test.ts`

**Interfaces:**
- Consumes: `ConfigStore` / `getLocaleStrings` / `WHITELIST_PRESETS`
- Produces: `SettingTabWhitelist`（タグUI・プリセットグリッド・追加/削除/リセット）

- [ ] **Step 1: i18n 失敗テスト**

`tests/core/i18n.test.ts` に追加:

```typescript
  it('whitelist ラベルを 3 言語で持つ', () => {
    expect(getLocaleStrings('ja').whitelistEnabled).toContain('有効');
    expect(getLocaleStrings('en').whitelistEnabled).toContain('Enable');
    expect(getLocaleStrings('zh').whitelistEnabled).toContain('启用');
  });
```

- [ ] **Step 2: 失敗を確認**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/core/i18n.test.ts
```

Expected: FAIL

- [ ] **Step 3: i18n.ts に whitelist キー追加**

`src/core/i18n.ts` の `LocaleStrings` に追加:

```typescript
  whitelistEnabled: string;
  whitelistEnabledDesc: string;
  whitelistExtensionsHeading: string;
  whitelistExtensionsDesc: string;
  whitelistAddExtension: string;
  whitelistAddExtensionDesc: string;
  whitelistAddExtensionPlaceholder: string;
  whitelistAddExtensionButton: string;
  whitelistPresetsHeading: string;
  whitelistPresetsDesc: string;
  whitelistApplyButton: string;
  whitelistAlwaysShowFolders: string;
  whitelistAlwaysShowFoldersDesc: string;
  whitelistReset: string;
  whitelistResetDesc: string;
  whitelistResetButton: string;
```

3 言語の STRINGS に追加（`comingSoon` 行の前に、office ラベル群の後に）:

```typescript
    // ja
    whitelistEnabled: '✅ 有効化',
    whitelistEnabledDesc: '拡張子フィルターのON/OFF',
    whitelistExtensionsHeading: '📋 許可する拡張子',
    whitelistExtensionsDesc: 'ここに登録した拡張子のファイルだけが左側一覧に表示されます',
    whitelistAddExtension: '追加する拡張子',
    whitelistAddExtensionDesc: 'ドットなしで入力（例: json, yaml, js）',
    whitelistAddExtensionPlaceholder: '拡張子名',
    whitelistAddExtensionButton: '＋ 追加',
    whitelistPresetsHeading: '🎯 プリセット',
    whitelistPresetsDesc: '現在のリストに追加する形式です。「ALL」はフィルターを解除します。',
    whitelistApplyButton: '適用',
    whitelistAlwaysShowFolders: '📁 フォルダは常に表示',
    whitelistAlwaysShowFoldersDesc: 'フォルダをフィルター対象外とする',
    whitelistReset: '🔄 デフォルトにリセット',
    whitelistResetDesc: 'すべての設定を初期状態に戻します',
    whitelistResetButton: 'リセット',
```

```typescript
    // en
    whitelistEnabled: '✅ Enable',
    whitelistEnabledDesc: 'Toggle the extension filter',
    whitelistExtensionsHeading: '📋 Allowed extensions',
    whitelistExtensionsDesc: 'Only files with these extensions appear in the file list',
    whitelistAddExtension: 'Add extension',
    whitelistAddExtensionDesc: 'Enter without dot (e.g. json, yaml, js)',
    whitelistAddExtensionPlaceholder: 'extension',
    whitelistAddExtensionButton: '+ Add',
    whitelistPresetsHeading: '🎯 Presets',
    whitelistPresetsDesc: 'Adds these to your current list. "ALL" disables the filter.',
    whitelistApplyButton: 'Apply',
    whitelistAlwaysShowFolders: '📁 Always show folders',
    whitelistAlwaysShowFoldersDesc: 'Folders are not affected by the filter',
    whitelistReset: '🔄 Reset to default',
    whitelistResetDesc: 'Restore all settings to defaults',
    whitelistResetButton: 'Reset',
```

```typescript
    // zh
    whitelistEnabled: '✅ 启用',
    whitelistEnabledDesc: '切换扩展名过滤',
    whitelistExtensionsHeading: '📋 允许的扩展名',
    whitelistExtensionsDesc: '仅显示列表中扩展名的文件',
    whitelistAddExtension: '添加扩展名',
    whitelistAddExtensionDesc: '不含点（例如 json, yaml, js）',
    whitelistAddExtensionPlaceholder: '扩展名',
    whitelistAddExtensionButton: '＋ 添加',
    whitelistPresetsHeading: '🎯 预设',
    whitelistPresetsDesc: '添加到当前列表。「ALL」会取消过滤。',
    whitelistApplyButton: '应用',
    whitelistAlwaysShowFolders: '📁 始终显示文件夹',
    whitelistAlwaysShowFoldersDesc: '文件夹不受过滤影响',
    whitelistReset: '🔄 恢复默认',
    whitelistResetDesc: '将所有设置恢复为默认值',
    whitelistResetButton: '重置',
```

- [ ] **Step 4: SettingTabWhitelist.ts を置換**

`src/settings/SettingTabWhitelist.ts`:

```typescript
import { App, Notice, Plugin, PluginSettingTab, Setting } from 'obsidian';
import type { ConfigStore } from '../core/config-store';
import { DEFAULT_WHITELIST_SETTINGS } from '../core/settings';
import { getLocaleStrings } from '../core/i18n';
import { WHITELIST_PRESETS } from '../features/whitelist/presets';

export class SettingTabWhitelist extends PluginSettingTab {
  constructor(app: App, plugin: Plugin, private store: ConfigStore) {
    super(app, plugin);
  }

  display(): void {
    const { containerEl } = this;
    containerEl.empty();
    const lang = (this.plugin as unknown as { env?: { language?: string } }).env?.language ?? 'en';
    const s = getLocaleStrings(lang);

    containerEl.createEl('h2', { text: s.tabWhitelist });
    containerEl.createEl('p', {
      text: s.whitelistExtensionsDesc,
      attr: { style: 'color: var(--text-muted); margin-bottom: 2em;' },
    });

    const cfg = this.store.load();

    // 有効化
    new Setting(containerEl)
      .setName(s.whitelistEnabled)
      .setDesc(s.whitelistEnabledDesc)
      .addToggle((t) => t.setValue(cfg.whitelist.enabled).onChange((v) => {
        try {
          const latest = this.store.load();
          this.store.save({ ...latest, whitelist: { ...latest.whitelist, enabled: v } });
          new Notice('✅ 保存しました');
        } catch (e) {
          new Notice(`⚠️ 保存失敗: ${(e as Error).message}`);
          this.display();
        }
      }));

    // 拡張子追加
    containerEl.createEl('h3', { text: s.whitelistExtensionsHeading });

    const addSetting = new Setting(containerEl)
      .setName(s.whitelistAddExtension)
      .setDesc(s.whitelistAddExtensionDesc)
      .addText((text) => {
        text.setPlaceholder(s.whitelistAddExtensionPlaceholder);
        text.inputEl.addEventListener('keydown', async (e) => {
          if (e.key === 'Enter') {
            await this.addExtension((e.target as HTMLInputElement).value);
          }
        });
      })
      .addButton((button) =>
        button.setButtonText(s.whitelistAddExtensionButton).onClick(async () => {
          const input = addSetting.controlEl.querySelector('input');
          if (input) await this.addExtension(input.value);
        })
      );

    // タグ
    if (cfg.whitelist.extensions.length === 0) {
      containerEl.createEl('p', {
        text: '（すべてのファイルが表示されます）',
        attr: { style: 'color: var(--text-muted); font-style: italic;' },
      });
    } else {
      const tagContainer = containerEl.createDiv('cb-whitelist-tags');
      cfg.whitelist.extensions.forEach((ext) => {
        const tag = tagContainer.createEl('span', { cls: 'cb-whitelist-tag' });
        tag.createEl('span', { text: `.${ext}` });
        const removeBtn = tag.createEl('span', { cls: 'cb-whitelist-tag-remove', text: '✕' });
        removeBtn.addEventListener('click', async () => {
          try {
            const latest = this.store.load();
            this.store.save({
              ...latest,
              whitelist: {
                ...latest.whitelist,
                extensions: latest.whitelist.extensions.filter((e) => e !== ext),
              },
            });
            this.display();
          } catch (e) {
            new Notice(`⚠️ ${(e as Error).message}`);
            this.display();
          }
        });
      });
    }

    // プリセット
    containerEl.createEl('h3', { text: s.whitelistPresetsHeading });
    containerEl.createEl('p', {
      text: s.whitelistPresetsDesc,
      attr: { style: 'color: var(--text-muted); font-size: 0.85em;' },
    });
    const presetGrid = containerEl.createDiv('cb-whitelist-presets');
    WHITELIST_PRESETS.forEach((preset) => {
      const card = presetGrid.createDiv('cb-whitelist-preset-card');
      card.createEl('div', { text: preset.name, attr: { style: 'font-weight: bold;' } });
      card.createEl('div', {
        text: preset.desc,
        attr: { style: 'font-size: 0.8em; color: var(--text-muted);' },
      });
      const btn = card.createEl('button', { text: s.whitelistApplyButton, attr: { style: 'margin-top: 6px; cursor: pointer;' } });
      btn.addEventListener('click', async () => {
        try {
          const latest = this.store.load();
          const next = preset.extensions[0] === '*'
            ? []
            : Array.from(new Set([...latest.whitelist.extensions, ...preset.extensions])).sort();
          this.store.save({ ...latest, whitelist: { ...latest.whitelist, extensions: next } });
          this.display();
        } catch (e) {
          new Notice(`⚠️ ${(e as Error).message}`);
          this.display();
        }
      });
    });

    // フォルダ常時表示
    containerEl.createEl('h3', { text: '⚙️ オプション' });
    new Setting(containerEl)
      .setName(s.whitelistAlwaysShowFolders)
      .setDesc(s.whitelistAlwaysShowFoldersDesc)
      .addToggle((t) => t.setValue(cfg.whitelist.alwaysShowFolders).onChange((v) => {
        try {
          const latest = this.store.load();
          this.store.save({ ...latest, whitelist: { ...latest.whitelist, alwaysShowFolders: v } });
        } catch (e) {
          new Notice(`⚠️ ${(e as Error).message}`);
          this.display();
        }
      }));

    // リセット
    containerEl.createEl('hr');
    new Setting(containerEl)
      .setName(s.whitelistReset)
      .setDesc(s.whitelistResetDesc)
      .addButton((b) => b.setButtonText(s.whitelistResetButton).setWarning().onClick(() => {
        try {
          const latest = this.store.load();
          this.store.save({ ...latest, whitelist: { ...DEFAULT_WHITELIST_SETTINGS } });
          this.display();
        } catch (e) {
          new Notice(`⚠️ ${(e as Error).message}`);
          this.display();
        }
      }));
  }

  private async addExtension(raw: string): Promise<void> {
    const ext = raw.trim().toLowerCase().replace(/^\./, '');
    if (!ext) return;
    const latest = this.store.load();
    if (latest.whitelist.extensions.includes(ext)) return;
    try {
      this.store.save({
        ...latest,
        whitelist: { ...latest.whitelist, extensions: [...latest.whitelist.extensions, ext] },
      });
      this.display();
    } catch (e) {
      new Notice(`⚠️ ${(e as Error).message}`);
      this.display();
    }
  }
}
```

- [ ] **Step 5: テスト + 型チェック**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm test -- tests/core/i18n.test.ts && npm run typecheck
```

Expected: i18n 6 件 PASS + 型チェック OK

- [ ] **Step 6: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/settings/SettingTabWhitelist.ts src/core/i18n.ts tests/core/i18n.test.ts && git commit -m "feat(settings): SettingTabWhitelist 本実装 + i18n whitelist ラベル"
```

---

### Task 8: スタイル追加

**Files:**
- Modify: `D:\AI-Agent\ClaudianBridge\styles.css`

- [ ] **Step 1: スタイル追記**

`styles.css` の末尾に追加:

```css
/* Whitelist tags */
.cb-whitelist-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin: 12px 0;
  padding: 8px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  background: var(--background-primary);
}
.cb-whitelist-tag {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 8px;
  background: var(--interactive-accent);
  color: var(--text-on-accent);
  border-radius: 4px;
  font-size: 0.85em;
  font-family: var(--font-monospace);
  transition: opacity 0.15s;
}
.cb-whitelist-tag:hover { opacity: 0.85; }
.cb-whitelist-tag-remove {
  cursor: pointer;
  font-size: 0.75em;
  opacity: 0.7;
  padding: 0 2px;
  line-height: 1;
}
.cb-whitelist-tag-remove:hover { opacity: 1; color: var(--text-error); }

/* Preset cards */
.cb-whitelist-presets {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  gap: 8px;
  margin: 12px 0;
}
.cb-whitelist-preset-card {
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  padding: 10px;
  background: var(--background-secondary);
  transition: background 0.15s;
}
.cb-whitelist-preset-card:hover { background: var(--background-modifier-hover); }
.cb-whitelist-preset-card button {
  background: var(--interactive-accent);
  color: var(--text-on-accent);
  border: none;
  border-radius: 4px;
  padding: 4px 12px;
  font-size: 0.8em;
  transition: opacity 0.15s;
}
.cb-whitelist-preset-card button:hover { opacity: 0.85; }
```

- [ ] **Step 2: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add styles.css && git commit -m "feat(styles): whitelist tag + preset card"
```

---

### Task 9: main.ts オーケストレーション

**Files:**
- Modify: `D:\AI-Agent\ClaudianBridge\src\main.ts`

**Interfaces:**
- Consumes: `SettingTabWhitelist`（Task 7）/ `installWhitelistCss` / `removeWhitelistCss` / `buildWhitelistCss`（Tasks 3-4）
- Produces: onload で whitelist CSS 注入、watch で再注入、onunload で remove

- [ ] **Step 1: main.ts を更新**

`src/main.ts` の import に追加:

```typescript
import { buildWhitelistCss } from './features/whitelist/css-builder';
import { installWhitelistCss, removeWhitelistCss } from './features/whitelist/injector';
```

`onload` 内、`this.addSettingTab(new SettingTabWhitelist(this.app, this));` を置換:

```typescript
    this.addSettingTab(new SettingTabWhitelist(this.app, this, this.store));
```

`// 2. 旧プラグイン無効化` の **後**、設定タブ登録の **前** に追加:

```typescript
    // 2.5 Whitelist CSS 注入（whitelist.enabled 時のみ）
    {
      const w = this.store.load().whitelist;
      if (w.enabled) {
        const css = buildWhitelistCss(w.extensions, w.alwaysShowFolders);
        if (css) installWhitelistCss(css);
      }
    }
```

`this.store.watch(() => { /* 外部変更時のフック */ });` を以下に置換（設定変更時に whitelist CSS 再注入）:

```typescript
    this.store.watch(() => {
      const w = this.store.load().whitelist;
      const css = w.enabled ? buildWhitelistCss(w.extensions, w.alwaysShowFolders) : null;
      if (css) installWhitelistCss(css); else removeWhitelistCss();
    });
```

`onunload` メソッドを置換:

```typescript
  onunload(): void {
    removeWhitelistCss();
    this.store.close();
    console.log('[claudian-bridge] unloaded');
  }
```

- [ ] **Step 2: ビルド + 全テスト**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm run build && npm test
```

Expected: ビルド成功・全テスト PASS（既存 + 新規）

- [ ] **Step 3: コミット**

```bash
cd "D:/AI-Agent/ClaudianBridge" && git add src/main.ts && git commit -m "feat(main): wire SettingTabWhitelist + CSS inject/unmount on settings change"
```

---

### Task 10: デプロイ + 実機 UAT

**Files:**
- デプロイ成果物: `.obsidian/plugins/claudian-bridge/`

- [ ] **Step 1: デプロイ**

```bash
cd "D:/AI-Agent/ClaudianBridge" && npm run deploy
```

Expected: 3 ファイルが `.obsidian/plugins/claudian-bridge/` にコピー

- [ ] **Step 2: デプロイ検証（文字列マーカー）**

```bash
grep -o "claudian-bridge\|cb-whitelist-css\|cb-whitelist-tag\|cb-whitelist-preset" "Z:/Obsidian Vault/.obsidian/plugins/claudian-bridge/main.js" | sort | uniq -c
grep -c "cb-whitelist-tags\|cb-whitelist-preset-card" "Z:/Obsidian Vault/.obsidian/plugins/claudian-bridge/styles.css"
```

Expected: main.js に `claudian-bridge` と `cb-whitelist-*` マーカー、styles.css に `cb-whitelist-*` クラスが存在

- [ ] **Step 3: 実機手動 UAT**

Obsidian を再起動し、以下を確認:

| # | 確認項目 | 期待結果 |
|---|---------|---------|
| 1 | 設定画面「拡張子フィルタ」タブに全項目表示 | ✅ |
| 2 | 拡張子追加（テキスト + Enter または +追加） | ✅ |
| 3 | 拡張子タグの ✕ で削除 | ✅ |
| 4 | プリセット「📄 Obsidian標準」等でリストに追加 | ✅ |
| 5 | 「📦 ALL」プリセット → フィルター解除（全表示） | ✅ |
| 6 | ファイル一覧で指定拡張子以外が非表示 | ✅ |
| 7 | フォルダ常時表示 on/off 切替 | ✅ |
| 8 | 旧 extension-whitelist 設定の自動移行 | ✅ |
| 9 | `_disabled__extension-whitelist/data.json.bak.json` バックアップ | ✅ |
| 10 | プラグイン無効化/再有効化で CSS 注入/削除 | ✅ |

- [ ] **Step 4: コミットなし（デプロイ成果物はプラグイン側 main.js）**

---

### Task 11: ドキュメント更新 + コミット

**Files:**
- Modify: `80_POC_Projects\POC_017_ClaudianBridge\README.md`
- Modify: `80_POC_Projects\POC_017_ClaudianBridge\CHANGELOG.md`
- Modify: `80_POC_Projects\POC_017_ClaudianBridge\03_開発文書\04_開発ログ.md`
- Modify: `80_POC_Projects\POC_017_ClaudianBridge\03_開発文書\05_進捗ボード.md`

- [ ] **Step 1: README 更新**

サブプロジェクト一覧の P4 を ✅ に変更:

```markdown
| P4 | 拡張子フィルタ統合 | F005 | ✅ 完成 |
```

- [ ] **Step 2: CHANGELOG v0.3.0 追記**

```markdown
## v0.3.0 (2026-08-10) — P4 拡張子フィルタ統合

> 🎯 今回の主題: extension-whitelist の F005（左サイドバー拡張子絞り込み）を Claudian Bridge に統合

### ✨ 主な内容
- 拡張子フィルタ（CSS注入による左ファイル一覧絞り込み）
- プリセット5種（Obsidian標準 / Office / 画像 / コード / ALL）
- タグUI（追加・✕削除）+ フォルダ常時表示 + リセット
- WhitelistSettings スキーマ + 旧 extension-whitelist data.json 自動移行
- 拡張子フィルタタブ本実装 + i18n（ja/zh/en）
```

- [ ] **Step 3: 開発ログ行追加**

`03_開発文書/04_開発ログ.md` のインデックスに追加:

```markdown
| 2026-08-10 | P4 拡張子フィルタ統合（F005 完全互換移植） | ✅ | [[../00_P4_実装計画|実装計画]] |
```

- [ ] **Step 4: 進捗ボード更新**

P4 を ✅、P5 を ⏳ に更新。P2/P3 と同様にサマリー表も更新。

- [ ] **Step 5: コミット**

```bash
cd "Z:/Obsidian Vault/80_POC_Projects/POC_017_ClaudianBridge"
git add README.md CHANGELOG.md "03_開発文書/04_開発ログ.md" "03_開発ボード.md"
git commit -m "docs(claudian-bridge): P4 完了記録を POC_017 に追加"
```

---

## 自审记录

- **Spec 覆盖**:
  - F005（拡張子フィルタ）→ Task 7, 9 ✅
  - プリセット5種 → Task 2 ✅
  - タグUI（追加/削除） → Task 7 ✅
  - データ移行 → Task 5, 6 ✅
  - 設定タブ + i18n → Task 7 ✅
  - ビルド & デプロイ → Task 10 ✅
  - ドキュメント → Task 11 ✅
- **プレースホルダ**: 全て実コード ✅
- **型整合性**: `WhitelistSettings` / `WhitelistPreset` / `buildWhitelistCss` / `installWhitelistCss` / `SettingTabWhitelist` が Task 間で一致 ✅
- **依存タスク**: Task 7 は 2/3/4/5、Task 9 は 4/7/8、Task 6 は 5 に依存 ✅
- **コミット粒度**: 10 commits（Task ごとに 1 commit）✅

## 📊 計画サマリー

| Task | 内容 | 主なファイル | コミット粒度 |
|------|------|------------|------------|
| 1 | WhitelistSettings スキーマ | src/core/settings.ts | 1 commit |
| 2 | プリセット5件 | src/features/whitelist/presets.ts | 1 commit |
| 3 | CSS ビルダー | src/features/whitelist/css-builder.ts | 1 commit |
| 4 | DOM 注入ラッパー | src/features/whitelist/injector.ts | 1 commit |
| 5 | convert-ew 変換 | src/legacy/convert-ew.ts | 1 commit |
| 6 | migration に extension-whitelist | src/legacy/migration.ts | 1 commit |
| 7 | SettingTabWhitelist + i18n | src/settings/SettingTabWhitelist.ts, src/core/i18n.ts | 1 commit |
| 8 | スタイル | styles.css | 1 commit |
| 9 | main.ts 配線 | src/main.ts | 1 commit |
| 10 | デプロイ + UAT | .obsidian/plugins/claudian-bridge/ | 1 commit(なし) |
| 11 | ドキュメント更新 | POC_017 docs | 1 commit |
| **計** | | | **10 commits** |

---

## 🚀 実行オプション

P4 計画完成 & `80_POC_Projects/POC_017_ClaudianBridge/03_開発文書/00_P4_実装計画.md` に保存済み。2つの実行方式：

1. **サブエージェント駆動（推奨）** — タスクごとに新しいサブエージェントを派遣し、タスク間でレビュー、高速なイテレーション
2. **インライン実行** — このセッション内で `executing-plans` を使って順次実行、チェックポイントごとに確認

どちらで進めますか？

---

*🛠️ Claudian Bridge P4 実装計画 v1.0.0 · POC_017 · MiuMiu 🐾 · 2026-08-10*

# 完了報告読上げスクリプト整形 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ✅ 完了報告の自動読上げ時に、報告ヘッダー・結論・次のアクション提案のサマリーのみを読み上げる読上げ用スクリプト整形機能を追加する（v0.28.0・F026）。

**Architecture:** 新規 `report-script.ts` がレンダリング後 DOM から「見出し語＋直下サマリー段落」を収集してスクリプト文字列を組み立てる。`extractReportText` の full スコープ冒頭で完了報告かつ設定 ON の場合に使用し、失敗時は従来の全文読上げへフォールバック。設定は `tts.autoReadReportScript`（既定 ON）。

**Tech Stack:** TypeScript / Obsidian Plugin API / vitest（jsdom）

**設計書:** [[02_設計文書/2026-08-30-report-speech-script-design]]

## Global Constraints

- 成果物・検証結果・参照文献・テーブルは**読み上げない**
- 読み上げ構成は `タスク完了です。` → 📢 ヘッダー → `結論。` → `次のアクション提案です。` の順で固定
- 既存 API は optional 引数 / optional フィールド追加のみで後方互換を維持
- 設定キーは `tts.autoReadReportScript`・既定 `true`・normalize で `typeof === 'boolean'` ガード
- F026 は commit 前に `02_設計文書/F-number_master.md` へ登録（付与規約）
- テスト実行: `npx vitest run <file>` / 全体: `npm test` / 型: `npm run typecheck`
- Vault ワークツリー（`80_POC_Projects/POC_017_ClaudianBridge`）は作業後 `git merge --ff-only main` で反映

---

### Task 1: report-script.ts（スクリプト組み立てコア）

**Files:**
- Create: `src/features/tts/report-script.ts`
- Modify: `src/features/tts/extract-report.ts`（`HEADING_SELECTOR` と `isSummaryMarker` を export）
- Test: `tests/features/tts/report-script.test.ts`（新規）

**Interfaces:**
- Consumes: `extract-report.ts` の `readVisibleTextExcluding(el, excludeSel): string` / `collectStructuralExcludes(el): Element[]`
- Produces: `isCompletionReport(source: Element): boolean` / `buildReportScript(source: Element, excludeSel: string): string | null`（Task 2 が利用）

- [ ] **Step 1: extract-report.ts の 2 定数を export に変更**

`src/features/tts/extract-report.ts` L111 と L114 を修正:

```ts
/** 見出し要素（markdown 見出し） */
export const HEADING_SELECTOR = 'h1, h2, h3, h4, h5, h6';

/** ヘッダースコープの「結果全体まとめ」マーカー（✅・📢 で始まる） */
export function isSummaryMarker(text: string): boolean {
```

- [ ] **Step 2: 失敗テストを書く**

`tests/features/tts/report-script.test.ts` を新規作成:

```ts
/**
 * v0.28.0 (F026): タスク完了報告 → 読上げ用スクリプト整形のテスト。
 * 設計書: 02_設計文書/2026-08-30-report-speech-script-design.md
 */
import { describe, it, expect } from 'vitest';
import { isCompletionReport, buildReportScript } from '../../../src/features/tts/report-script';

/**
 * 報告 1 件分の DOM を組み立てるヘルパー。
 * innerText 非対応環境（jsdom）向けに textContent が正しく返る構造にする。
 */
function makeReport(): HTMLElement {
  const root = document.createElement('div');
  root.innerHTML = `
    <h2>✅ 完了 · 修正タスク · 2026-08-30 · 🟡 M</h2>
    <blockquote><p>📢 修正を完了しました。テスト 790 件 PASS です。</p></blockquote>
    <h3>🎯 結論</h3>
    <p>読上げの言語不具合 2 件がすべて解決しました。</p>
    <table><tr><td>作業</td><td>状態</td></tr></table>
    <h3>🎁 成果物</h3>
    <p>修正コミットとタグを作成しました。</p>
    <h3>✅ 検証結果</h3>
    <table><tr><td>テスト</td><td>✅ PASS</td></tr></table>
    <h3>📚 参照文献</h3>
    <table><tr><td>1</td><td>LLM</td></tr></table>
    <h3>🔜 次のアクション提案</h3>
    <p>残りの読上げ問題 2 件の調査をお勧めします。</p>
    <table><tr><td>1</td><td>調査</td></tr></table>
  `;
  return root;
}

describe('isCompletionReport', () => {
  it('✅ 完了見出しがあれば true', () => {
    expect(isCompletionReport(makeReport())).toBe(true);
  });
  it('完了見出しが無ければ false', () => {
    const root = document.createElement('div');
    root.innerHTML = '<h3>🎯 結論</h3><p>普通の回答です。</p>';
    expect(isCompletionReport(root)).toBe(false);
  });
});

describe('buildReportScript', () => {
  it('完了報告を ヘッダー/結論/次のアクション提案 のスクリプトに変換する', () => {
    const script = buildReportScript(makeReport(), '');
    expect(script).toBe(
      [
        'タスク完了です。',
        '📢 修正を完了しました。テスト 790 件 PASS です。',
        '結論。 読上げの言語不具合 2 件がすべて解決しました。',
        '次のアクション提案です。 残りの読上げ問題 2 件の調査をお勧めします。',
      ].join('\n'),
    );
  });

  it('成果物・検証結果・参照文献・テーブルは含まない', () => {
    const script = buildReportScript(makeReport(), '') ?? '';
    expect(script).not.toContain('成果物');
    expect(script).not.toContain('検証結果');
    expect(script).not.toContain('参照文献');
    expect(script).not.toContain('PASS</td>');
    expect(script).not.toContain('修正コミットとタグ');
  });

  it('サマリー欠落の章は読み飛ばす', () => {
    const root = document.createElement('div');
    root.innerHTML = `
      <h2>✅ 完了 · タスク · 2026-08-30 · 🟢 S</h2>
      <blockquote><p>📢 完了しました。</p></blockquote>
      <h3>🎯 結論</h3>
      <table><tr><td>項目</td><td>状態</td></tr></table>
      <h3>🔜 次のアクション提案</h3>
      <p>次は実機確認をお勧めします。</p>
    `;
    const script = buildReportScript(root, '') ?? '';
    expect(script).toContain('タスク完了です。');
    expect(script).not.toContain('結論。');
    expect(script).toContain('次のアクション提案です。 次は実機確認をお勧めします。');
  });

  it('📢・結論・次のアクション提案が全て空なら null（フォールバック）', () => {
    const root = document.createElement('div');
    root.innerHTML = '<h2>✅ 完了 · タスク · 2026-08-30 · 🟢 S</h2><p>本文のみ。</p>';
    expect(buildReportScript(root, '')).toBe(null);
  });
});
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `npx vitest run tests/features/tts/report-script.test.ts`
Expected: FAIL（`report-script` モジュールが存在しない）

- [ ] **Step 4: report-script.ts を実装**

`src/features/tts/report-script.ts` を新規作成:

```ts
/**
 * v0.28.0 (F026): タスク完了報告 → 読上げ用スクリプト整形。
 * ✅ 完了報告から 報告ヘッダー（📢）・結論・次のアクション提案のサマリーのみを収集し、
 * TTS で自然に聴こえる敬体スクリプトを組み立てる。
 * 成果物・検証結果・参照文献・テーブルは読み上げない。
 * 設計書: 02_設計文書/2026-08-30-report-speech-script-design.md
 */
import {
  readVisibleTextExcluding,
  collectStructuralExcludes,
  HEADING_SELECTOR,
  isSummaryMarker,
} from './extract-report';

/** 読み上げるセクション（見出しテキスト完全一致 → 見出し語） */
const SECTION_SPECS: Array<{ heading: string; label: string }> = [
  { heading: '🎯 結論', label: '結論。' },
  { heading: '🔜 次のアクション提案', label: '次のアクション提案です。' },
];

/** サマリー収集を打ち切る要素（見出し・表・引用・思考・コード・ツール・コールアウト） */
const STOP_SELECTOR = `${HEADING_SELECTOR}, table, blockquote, .claudian-thinking-block, .claudian-code-wrapper, .claudian-tool-call, .callout`;

/** ✅ 完了報告（「✅」で始まり 完了/修正/実装 を含む見出しが存在）か判定する */
export function isCompletionReport(source: Element): boolean {
  return Array.from(source.querySelectorAll(HEADING_SELECTOR)).some((h) => {
    const t = (h.textContent ?? '').trim();
    return t.startsWith('✅') && (t.includes('完了') || t.includes('修正') || t.includes('実装'));
  });
}

/** 見出し直下の最初の非空段落（サマリー）を取得する。終端条件に当たったら空文字 */
function readSectionSummary(source: Element, heading: Element, excludeSel: string): string {
  const hidden = collectStructuralExcludes(source);
  hidden.push(heading);
  const mark = 'data-cb-temp-hide';
  hidden.forEach((e) => e.setAttribute(mark, '1'));
  try {
    let next: Element | null = heading.nextElementSibling;
    while (next && !next.matches(STOP_SELECTOR)) {
      const text = readVisibleTextExcluding(next, `${excludeSel}, [${mark}]`);
      if (text !== '') return text;
      next = next.nextElementSibling;
    }
    return '';
  } finally {
    hidden.forEach((e) => e.removeAttribute(mark));
  }
}

/**
 * 完了報告 DOM から読上げ用スクリプトを組み立てる。
 * 収集できる要素が無い場合は null を返し、呼び出し側は従来の全文読上げへフォールバックする。
 */
export function buildReportScript(source: Element, excludeSel: string): string | null {
  const parts: string[] = [];

  const report = Array.from(source.querySelectorAll('blockquote'))
    .find((b) => isSummaryMarker((b.textContent ?? '').trim()));
  if (report) {
    const text = readVisibleTextExcluding(report, excludeSel);
    if (text !== '') parts.push(text);
  }

  for (const { heading, label } of SECTION_SPECS) {
    const h = Array.from(source.querySelectorAll(HEADING_SELECTOR))
      .find((e) => (e.textContent ?? '').trim() === heading);
    if (!h) continue;
    const summary = readSectionSummary(source, h, excludeSel);
    if (summary !== '') parts.push(`${label} ${summary}`);
  }

  if (parts.length === 0) return null;
  return ['タスク完了です。', ...parts].join('\n');
}
```

- [ ] **Step 5: テストが成功することを確認**

Run: `npx vitest run tests/features/tts/report-script.test.ts`
Expected: PASS（6 件）

- [ ] **Step 6: 全テストで既存影響がないことを確認**

Run: `npm test`
Expected: PASS（既存 790 件 + 新規 6 件 = 796 件）

- [ ] **Step 7: コミット**

```bash
git add src/features/tts/report-script.ts src/features/tts/extract-report.ts tests/features/tts/report-script.test.ts
git commit -m "feat(F026): 完了報告→読上げ用スクリプト整形コア (report-script.ts)"
```

---

### Task 2: extractReportText の full スコープ分岐

**Files:**
- Modify: `src/features/tts/extract-report.ts`（`extractReportText` の full スコープ冒頭 + opts 型）
- Test: `tests/features/tts/report-script.test.ts`（extractReportText 分岐テストを追記）

**Interfaces:**
- Consumes: Task 1 の `isCompletionReport` / `buildReportScript`
- Produces: `extractReportText(messagesEl, scope, opts?)` の `opts` に `reportScript?: boolean`（既定 false）を追加。Task 4 の auto-read 呼び出し側が利用

- [ ] **Step 1: 失敗テストを追記**

`tests/features/tts/report-script.test.ts` の末尾に追記:

```ts
import { extractReportText } from '../../../src/features/tts/extract-report';

/** extractReportText へ渡す .claudian-messages 相当のラッパ */
function makeMessages(report: HTMLElement): Element {
  const messages = document.createElement('div');
  const assistant = document.createElement('div');
  assistant.className = 'claudian-message-assistant';
  const content = document.createElement('div');
  content.className = 'claudian-message-content';
  const block = document.createElement('div');
  block.className = 'claudian-text-block';
  while (report.firstChild) block.appendChild(report.firstChild);
  content.appendChild(block);
  assistant.appendChild(content);
  messages.appendChild(assistant);
  return messages;
}

describe('extractReportText reportScript オプション', () => {
  it('reportScript: true + 完了報告 → スクリプトを返す', () => {
    const messages = makeMessages(makeReport());
    const text = extractReportText(messages, 'full', { reportScript: true });
    expect(text).toContain('タスク完了です。');
    expect(text).toContain('結論。');
  });

  it('reportScript: false → 従来どおり全文を返す', () => {
    const messages = makeMessages(makeReport());
    const text = extractReportText(messages, 'full', { reportScript: false });
    expect(text).toContain('成果物');
    expect(text).not.toContain('タスク完了です。');
  });

  it('完了報告でない通常回答は reportScript: true でも全文を返す', () => {
    const root = document.createElement('div');
    root.innerHTML = '<h3>🎯 結論</h3><p>普通の回答です。</p>';
    const messages = makeMessages(root);
    const text = extractReportText(messages, 'full', { reportScript: true });
    expect(text).toContain('普通の回答です。');
    expect(text).not.toContain('タスク完了です。');
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run tests/features/tts/report-script.test.ts`
Expected: FAIL（`reportScript` オプションが未実装のため 2 件目以降が期待どおり動かない）

- [ ] **Step 3: extractReportText に分岐を実装**

`src/features/tts/extract-report.ts`:

1. 冒頭に import を追加:

```ts
import { isCompletionReport, buildReportScript } from './report-script';
```

2. `extractReportText` の opts 型に追加（L213 付近）:

```ts
opts?: { excludeCallouts?: boolean; filter?: SpeechFilterOptions; reportScript?: boolean },
```

3. full スコープ分岐（L230-236）を置き換え:

```ts
  // v0.13.0: full scope は 📢 有無に関わらず最後の応答を全文読み上げ（全応答統一）
  if (scope === 'full') {
    if (last.hasAttribute(AUTO_READ_MARK)) return null;
    last.setAttribute(AUTO_READ_MARK, '1');
    const source = last.querySelector('.claudian-message-content') ?? last;
    // v0.28.0 (F026): 完了報告は読上げ用スクリプトに整形（失敗時は従来の全文へフォールバック）
    if (opts?.reportScript && isCompletionReport(source)) {
      const script = buildReportScript(source, speechExclude);
      if (script !== null && script !== '') return script;
    }
    const text = extractTextBlocks(source, speechExclude);
    return text === '' ? null : text;
  }
```

- [ ] **Step 4: テストが成功することを確認**

Run: `npx vitest run tests/features/tts/report-script.test.ts tests/features/tts/auto-read.test.ts tests/features/tts/integration.test.ts`
Expected: PASS（既存 auto-read / integration への影響なし）

- [ ] **Step 5: コミット**

```bash
git add src/features/tts/extract-report.ts tests/features/tts/report-script.test.ts
git commit -m "feat(F026): extractReportText full スコープに完了報告スクリプト分岐を追加"
```

---

### Task 3: 設定 autoReadReportScript 追加

**Files:**
- Modify: `src/core/settings.ts`（型 L515 付近 / 既定値 L568 付近 / normalize L772-830 付近）
- Test: `tests/core/settings.test.ts`

**Interfaces:**
- Consumes: なし
- Produces: `TtsSettings.autoReadReportScript?: boolean`（既定 `true`）。Task 4 が `cfg.tts.autoReadReportScript` として利用

- [ ] **Step 1: 失敗テストを追記**

`tests/core/settings.test.ts` の TTS 正規化 describe 内（`autoReadLanguageMode` のテスト近く）に追記:

```ts
describe('normalizeTtsSettings autoReadReportScript (v0.28.0)', () => {
  it('未設定時は既定 true', () => {
    const tts = normalizeTtsSettings({});
    expect(tts.autoReadReportScript).toBe(true);
  });
  it('false を指定した場合は false を保持', () => {
    const tts = normalizeTtsSettings({ autoReadReportScript: false });
    expect(tts.autoReadReportScript).toBe(false);
  });
  it('boolean 以外は既定 true', () => {
    const tts = normalizeTtsSettings({ autoReadReportScript: 'yes' });
    expect(tts.autoReadReportScript).toBe(true);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npx vitest run tests/core/settings.test.ts`
Expected: FAIL（`autoReadReportScript` 未定義 / normalize 関数名はファイル内の実名に合わせること。既存テストの import 文を参照）

- [ ] **Step 3: settings.ts に実装**

1. TtsSettings 型（L515 付近、`autoReadLanguageMode?: TtsLanguageMode;` の下）に追加:

```ts
  /** v0.28.0 (F026): 完了報告を読上げ用スクリプトに整形（既定 ON） */
  autoReadReportScript?: boolean;
```

2. 既定値（L568 付近、`autoReadLanguageMode: 'auto' as TtsLanguageMode,` の下）に追加:

```ts
  autoReadReportScript: true,
```

3. normalize 関数: `Partial<{...}>` 型に `autoReadReportScript: boolean;` を追加し、return オブジェクトに追加:

```ts
    ...(typeof r.autoReadReportScript === 'boolean' ? { autoReadReportScript: r.autoReadReportScript } : {}),
```

（Step 1 のテスト「未設定時は既定 true」を満たすため、未設定時は normalize の既定値オブジェクト生成箇所で補填される。既存の `autoReadLanguageMode` と同じパターンに従うこと）

- [ ] **Step 4: テストが成功することを確認**

Run: `npx vitest run tests/core/settings.test.ts`
Expected: PASS（追記 3 件含む）

- [ ] **Step 5: コミット**

```bash
git add src/core/settings.ts tests/core/settings.test.ts
git commit -m "feat(F026): tts.autoReadReportScript 設定を追加（既定 ON）"
```

---

### Task 4: i18n・設定画面・自動読上げの配線

**Files:**
- Modify: `src/core/i18n.ts`（interface L117 付近 + ja/zh/en の 3 ロケールブロック）
- Modify: `src/settings/SettingTabTts.ts`（L623 付近の excludeCallouts トグルの後）
- Modify: `src/features/tts/auto-read.ts`（L123 の extractReportText 呼び出し）
- Test: `tests/core/i18n.test.ts` / `tests/settings/SettingTabTts.test.ts`

**Interfaces:**
- Consumes: Task 2 の `opts.reportScript` / Task 3 の `cfg.tts.autoReadReportScript`
- Produces: ユーザー向け設定トグル＋自動読上げへの配線（完了）

- [ ] **Step 1: i18n キーを追加**

`src/core/i18n.ts`:

1. 文字列インターフェース（`ttsExcludeCallouts: string;` の下・L117 付近）に追加:

```ts
  ttsReportScript: string;
  ttsReportScriptDesc: string;
```

2. ja ブロック（`ttsExcludeCallouts: 'コールアウトを読み上げ対象から除外',` の近く）に追加:

```ts
    ttsReportScript: '完了報告を読み上げ用スクリプトに整形',
    ttsReportScriptDesc: '✅ 完了報告の自動読上げ時に、報告ヘッダー・結論・次のアクション提案のサマリーのみを読み上げます（成果物・検証結果・参照文献は読み上げません）',
```

3. zh ブロックに追加:

```ts
    ttsReportScript: '将任务完成报告整形为朗读脚本',
    ttsReportScriptDesc: '自动朗读 ✅ 完成报告时，仅朗读报告头部、结论与下一步建议的摘要（成果物、验证结果与参考文献不朗读）',
```

4. en ブロックに追加:

```ts
    ttsReportScript: 'Shape completion reports into a speech script',
    ttsReportScriptDesc: 'When auto-reading a ✅ completion report, read only the header, conclusion, and next-action summaries (deliverables, verification, and references are skipped)',
```

- [ ] **Step 2: i18n テストを確認・追記**

Run: `npx vitest run tests/core/i18n.test.ts`
既存テストが「全ロケールでキー一致」を検証している場合は追記不要（自動検証される）。キー漏れがあれば FAIL になるため修正する。検証されていない場合は以下を追記:

```ts
it('v0.28.0: ttsReportScript は全ロケールで定義される', () => {
  for (const strings of allLocaleStrings()) {
    expect(strings.ttsReportScript).toBeTruthy();
    expect(strings.ttsReportScriptDesc).toBeTruthy();
  }
});
```

（`allLocaleStrings()` はファイル内の既存ヘルパー名に合わせる。無ければ各ロケールの `getLocaleStrings()` 呼び出しを列挙する）

- [ ] **Step 3: SettingTabTts にトグルを追加**

`src/settings/SettingTabTts.ts` L623 付近の `excludeCallouts` トグル `new Setting(arBox)...` ブロックの直後に追加:

```ts
      new Setting(arBox)
        .setName(s.ttsReportScript)
        .setDesc(s.ttsReportScriptDesc)
        .addToggle((t) => t.setValue(cfg.tts.autoReadReportScript ?? true).onChange((v) => {
          const latest = store.load();
          store.save({ ...latest, tts: { ...latest.tts, autoReadReportScript: v } });
          draw();
        }));
```

- [ ] **Step 4: auto-read.ts で設定を渡す**

`src/features/tts/auto-read.ts` L123 の呼び出しを変更:

```ts
        const text = extractReportText(messages, scope, {
          excludeCallouts: cfg.tts.excludeCallouts ?? true,
          filter: resolveSpeechFilter(cfg, 'autoRead'),
          reportScript: cfg.tts.autoReadReportScript ?? true,  // v0.28.0 (F026)
        });
```

- [ ] **Step 5: SettingTabTts テストに失敗テストを追加し成功を確認**

`tests/settings/SettingTabTts.test.ts` — `excludeCallouts: false` の設定フィクスチャ（L116・L273 付近）と同パターンで、`autoReadReportScript: false` を設定したときにストアへ保存されることを検証するテストを追記する。トグル onChange のテストパターンは同ファイル内の既存トグルテスト（`excludeCallouts` 関連）をそのまま踏襲すること。

Run: `npx vitest run tests/settings/SettingTabTts.test.ts tests/core/i18n.test.ts`
Expected: PASS

- [ ] **Step 6: 全テスト・型チェック**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 7: コミット**

```bash
git add src/core/i18n.ts src/settings/SettingTabTts.ts src/features/tts/auto-read.ts tests/core/i18n.test.ts tests/settings/SettingTabTts.test.ts
git commit -m "feat(F026): 完了報告スクリプト整形の設定トグル・i18n・自動読上げ配線"
```

---

### Task 5: リリース（v0.28.0）

**Files:**
- Modify: `package.json` / `src/manifest.json` / `versions.json` / `package-lock.json`（バージョン 0.27.3 → 0.28.0）
- Modify: `CHANGELOG.md` / `README.md` / `08_説明書/03_リリースノート/リリースノート.md` / `08_説明書/03_リリースノート/バージョン履歴.md` / `02_設計文書/F-number_master.md` / `01_要件定義/00_要件総覧.md` / `01_要件定義/01_機能要件.md`

**Interfaces:**
- Consumes: Task 1〜4 の実装完了
- Produces: v0.28.0 リリース（タグ付与・Vault デプロイ済み）

- [ ] **Step 1: F026 を F-番号マスターへ登録（付与規約: commit 前に必須）**

`02_設計文書/F-number_master.md` の一覧表末尾（F025 の下）に追加:

```markdown
| F026 | 完了報告読上げスクリプト整形 | v0.28.0 | ✅ 完了報告を ヘッダー/結論/次のアクション提案 のサマリーのみ読み上げ（成果物・検証結果・参照文献・表は除外） | `[[2026-08-30-report-speech-script-design]]` |
```

見出しの「F001 〜 F025」表記も「F001 〜 F026」に更新すること。

- [ ] **Step 2: バージョンを 0.28.0 に更新**

```bash
sed -i 's/"version": "0.27.3"/"version": "0.28.0"/' package.json src/manifest.json
sed -i 's/"0.27.3": "1.7.2",/"0.28.0": "1.7.2",\n  "0.27.3": "1.7.2",/' versions.json
npm install --package-lock-only
```

- [ ] **Step 3: CHANGELOG.md に v0.28.0 エントリを追記**

`# Changelog` の直下に挿入。テスト件数は Task 1〜4 完了後の実測値（790 + 新規分）を記載すること:

```markdown
## [0.28.0] - 2026-08-30 — 完了報告の読上げ用スクリプト整形（F026）

### Added

- **完了報告を読上げ用スクリプトに整形**: ✅ 完了報告の自動読上げ時に「タスク完了です。」→ 📢 ヘッダー → 「結論。」→「次のアクション提案です。」のサマリーのみを読み上げる（`tts.autoReadReportScript`・既定 ON）
  - 成果物・検証結果・参照文献・テーブル・思考・コード・ツール呼び出しは読み上げない
  - 完了報告でない通常回答・整形失敗時は従来どおりの全文読上げへフォールバック
  - 新規設定: テキスト読み上げタブ「完了報告を読み上げ用スクリプトに整形」（i18n 3 言語）
```

- [ ] **Step 4: テスト全件・typecheck**

Run: `npm test && npm run typecheck`
Expected: 全 PASS・実測件数を CHANGELOG に反映

- [ ] **Step 5: ビルド＋Vault デプロイ＋検証**

```bash
npm run build
cat "/c/Users/superlambkin/OneDrive/Edge/Obsidian Vault/.obsidian/plugins/ClaudianBridge/VERSION"
```
Expected: `0.28.0`（manifest.json も同値）

- [ ] **Step 6: 実装コミット**

```bash
git add -A -- ':!MyPOC開発'
git commit -m "feat(F026): 完了報告読上げスクリプト整形 v0.28.0"
```

- [ ] **Step 7: POC 文書更新（README / リリースノート / バージョン履歴 / 要件総覧 / 機能要件）**

| 文書 | 更新内容 |
|------|---------|
| `README.md` | 現在バージョン v0.28.0・manifest v0.28.0・テスト実績実測値・F026 行を機能一覧へ追加 |
| `08_説明書/03_リリースノート/リリースノート.md` | v0.28.0 セクション追加（version frontmatter も +0.1） |
| `08_説明書/03_リリースノート/バージョン履歴.md` | v0.28.0 行追加（Minor） |
| `01_要件定義/00_要件総覧.md` / `01_要件定義/01_機能要件.md` | F026 行と詳細セクションを追加（既存 F025 のパターンに従う） |

frontmatter `modified: 2026-08-30` を更新し、コミット:

```bash
git add README.md 08_説明書/03_リリースノート/ 01_要件定義/
git commit -m "docs(POC_017): v0.28.0 リリース文書を反映"
git tag v0.28.0
```

- [ ] **Step 8: Vault ワークツリーへ反映**

```bash
cd "/c/Users/superlambkin/OneDrive/Edge/Obsidian Vault/80_POC_Projects/POC_017_ClaudianBridge"
git merge --ff-only main
```
Expected: fast-forward 成功（CRLF のみの差分で abort された場合は該当ファイルを `git checkout -- <file>` してから再実行）

---

## セルフレビュー記録（2026-08-30 実施）

| # | チェック | 結果 |
|:-:|---------|:----:|
| 1 | 設計書の要件 3 件（適用範囲/読む章/設定トグル）のカバー | ✅ Task 1・2（範囲）/ Task 1・2（3 要素・成果物除外）/ Task 3・4（トグル） |
| 2 | プレースホルダー（TBD / 適切にエラー処理 等） | ✅ 無し（Step 内に実コードを記載） |
| 3 | 型整合（reportScript / autoReadReportScript / isCompletionReport の名前） | ✅ Task 1→2→4 で一貫 |
| 4 | F026 付与規約（commit 前にマスター登録） | ✅ Task 5 Step 1 |

---

*📋 実装計画 v1.0.0 · ClaudianBridge POC_017 · F026 · MiuMiu 🐾 · 2026-08-30*

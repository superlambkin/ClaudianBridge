---
title: "Claudian Bridge オブジェクトコンテキストメニュー実装計画"
type: implementation-plan
version: 1.0.0
status: ✅ 已批准
created: 2026-08-11
modified: 2026-08-11
project_id: POC_017_ClaudianBridge
phase: 3
related_design:
  - 80_POC_Projects/POC_017_ClaudianBridge/02_設計文書/10_オブジェクトコンテキストメニュー設計.md
tags:
  - 実装計画
  - オブジェクトコンテキストメニュー
  - TDD
language: Japanese
applied_rules_version: 2.9.2
---

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

# 🛠️ Claudian Bridge オブジェクトコンテキストメニュー実装計画

**Goal:** Obsidian 標準 UI 上のオブジェクトを右クリック → ネイティブ context menu に「Add to Claudian」を追加 → 構造化マークダウンを Claudian チャット入力欄に挿入する。

**Architecture:** TypeScript ソース (`D:\AI-Agent\ClaudianBridge\src\features\object\`) に 4 つのモジュール (inspector / formatter / context-menu / settings) を追加。TDD で開発 → esbuild → `.obsidian/plugins/claudian-bridge/main.js` にデプロイ。既存の `selection/` パターン (core + popup + watcher) を踏襲。

**Tech Stack:** TypeScript / Obsidian Plugin API (Menu class / registerDomEvent) / vitest / esbuild / DOM API

## Global Constraints

- ソースリポジトリ: `D:\AI-Agent\ClaudianBridge\`（git main、feature ブランチ `feat/object-context-menu` で作業）
- 開発言語: TypeScript（ES2022 / strict）
- テストランナー: vitest（`npm test`）
- ビルド: `npm run build`（esbuild production）
- デプロイ: `npm run deploy` → `.obsidian/plugins/claudian-bridge/`
- 既存パターン踏襲: `src/features/selection/{core,popup,watcher}.ts` と同構造
- プラグイン本体は触らない (`.obsidian/plugins/claudian-bridge/main.js` は build 成果物)
- 設定 schema は `selection.*` 配下に追加（`selection.objectMenuEnabled` / `selection.objectMenuExcludeSelectors`）
- 設定 UI は `tabSelection` の「Selection popup」セクション末尾に追加
- manifest.json バージョン: `0.1.0` → `0.2.0`
- Vault 共通の MD 生成ルール（frontmatter / Mermaid / 双鏈）に準拠

---

## File Structure

| ファイル | 役割 |
|---------|------|
| **Create** `src/features/object/inspector.ts` | DOM 要素 → ObjectInfo 抽出の pure function |
| **Create** `src/features/object/formatter.ts` | ObjectInfo → 構造化マークダウン文字列変換 |
| **Create** `src/features/object/context-menu.ts` | `Menu` 構築 + `registerDomEvent('contextmenu')` 配線 |
| **Create** `src/features/object/index.ts` | 公開 API (registerObjectContextMenu) の re-export |
| **Modify** `src/core/settings.ts` | `selection.objectMenuEnabled` / `selection.objectMenuExcludeSelectors` 型追加 |
| **Modify** `src/core/config-store.ts` | デフォルト値 + 正規化 |
| **Modify** `src/settings/SettingTabSelection.ts` | 新サブセクション UI |
| **Modify** `src/main.ts` | `registerObjectContextMenu` 呼び出し |
| **Modify** `manifest.json` | `version: "0.2.0"` |
| **Create** `tests/features/object/inspector.test.ts` | ObjectInspector の単体テスト |
| **Create** `tests/features/object/formatter.test.ts` | ObjectFormatter の単体テスト |
| **Create** `tests/features/object/context-menu.test.ts` | 純粋ロジック (predicate) の単体テスト |

---

### Task 1: 設定スキーマ拡張 (TDD)

**Files:**
- Modify: `src/core/settings.ts`
- Modify: `src/core/config-store.ts`
- Create: `tests/core/config-store.test.ts` (or extend existing)

**Interfaces:**
- Consumes: 既存の `ClaudianBridgeSettings['selection']` 型
- Produces: `selection.objectMenuEnabled: boolean`、`selection.objectMenuExcludeSelectors: string[]`

- [ ] **Step 1: 失敗するテストを書く**

`tests/core/config-store.test.ts` に追加（または新規作成）:

```typescript
import { describe, it, expect } from 'vitest';
import { normalizeSettings } from '../../../src/core/config-store';

describe('normalizeSettings - object context menu', () => {
  it('objectMenuEnabled のデフォルトは true', () => {
    const s = normalizeSettings({});
    expect(s.selection.objectMenuEnabled).toBe(true);
  });

  it('objectMenuExcludeSelectors のデフォルトは 4 個', () => {
    const s = normalizeSettings({});
    expect(s.selection.objectMenuExcludeSelectors).toEqual([
      '.cb-popup',
      '.claudian-popup',
      '.menu',
      '.suggestion-container',
    ]);
  });

  it('不正な objectMenuEnabled (string) は boolean に正規化される', () => {
    const s = normalizeSettings({ selection: { objectMenuEnabled: 'yes' as unknown as boolean } });
    expect(s.selection.objectMenuEnabled).toBe(true);
  });

  it('objectMenuExcludeSelectors が配列でない場合は デフォルトに fallback', () => {
    const s = normalizeSettings({ selection: { objectMenuExcludeSelectors: 'bad' as unknown as string[] } });
    expect(s.selection.objectMenuExcludeSelectors).toEqual([
      '.cb-popup', '.claudian-popup', '.menu', '.suggestion-container',
    ]);
  });
});
```

- [ ] **Step 2: テスト実行 → FAIL 確認**

```bash
cd D:\AI-Agent\ClaudianBridge
npm test -- tests/core/config-store.test.ts
```

Expected: `TypeError: Cannot read properties of undefined (reading 'objectMenuEnabled')` 等、4 件失敗。

- [ ] **Step 3: 設定型を拡張**

`src/core/settings.ts`:

```typescript
export interface SelectionSettings {
  enabled: boolean;
  folderEnabled: boolean;
  delayMs: number;
  // === object context menu (v0.2.0) ===
  objectMenuEnabled: boolean;
  objectMenuExcludeSelectors: string[];
}
```

- [ ] **Step 4: config-store.ts の DEFAULT_SETTINGS + normalize を更新**

`src/core/config-store.ts`:

```typescript
const DEFAULT_OBJECT_EXCLUDE_SELECTORS = [
  '.cb-popup',
  '.claudian-popup',
  '.menu',
  '.suggestion-container',
];

// DEFAULT_SETTINGS.selection に追加:
const DEFAULT_SETTINGS: ClaudianBridgeSettings = {
  // ...既存...
  selection: {
    enabled: true,
    folderEnabled: true,
    delayMs: 300,
    objectMenuEnabled: true,
    objectMenuExcludeSelectors: [...DEFAULT_OBJECT_EXCLUDE_SELECTORS],
  },
};

// normalizeSettings 内:
function normalizeSelection(raw: unknown): SelectionSettings {
  const obj = (raw && typeof raw === 'object' ? raw : {}) as Partial<SelectionSettings>;
  return {
    enabled: typeof obj.enabled === 'boolean' ? obj.enabled : true,
    folderEnabled: typeof obj.folderEnabled === 'boolean' ? obj.folderEnabled : true,
    delayMs: Number.isInteger(obj.delayMs) && obj.delayMs >= 0 ? obj.delayMs : 300,
    objectMenuEnabled: typeof obj.objectMenuEnabled === 'boolean' ? obj.objectMenuEnabled : true,
    objectMenuExcludeSelectors: Array.isArray(obj.objectMenuExcludeSelectors)
      ? obj.objectMenuExcludeSelectors.filter((s): s is string => typeof s === 'string')
      : [...DEFAULT_OBJECT_EXCLUDE_SELECTORS],
  };
}
```

- [ ] **Step 5: テスト実行 → PASS 確認**

```bash
npm test -- tests/core/config-store.test.ts
```

Expected: 4 件 all PASS。

- [ ] **Step 6: Commit**

```bash
git add src/core/settings.ts src/core/config-store.ts tests/core/config-store.test.ts
git commit -m "feat(settings): add objectMenuEnabled + objectMenuExcludeSelectors"
```

---

### Task 2: ObjectInspector 実装 (TDD)

**Files:**
- Create: `src/features/object/inspector.ts`
- Create: `tests/features/object/inspector.test.ts`

**Interfaces:**
- Consumes: DOM `Element`
- Produces: `ObjectInfo | null`（null = 意味なし要素）

- [ ] **Step 1: 失敗するテストを書く**

`tests/features/object/inspector.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { getMeaningfulInfo, type ObjectInfo } from '../../../src/features/object/inspector';

function el(tag: string, attrs: Record<string, string> = {}): HTMLElement {
  const e = document.createElement(tag);
  for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
  return e;
}

describe('getMeaningfulInfo', () => {
  it('aria-label あり → name と selector を返す', () => {
    const e = el('button', { 'aria-label': 'Save', class: 'btn-primary' });
    const info = getMeaningfulInfo(e);
    expect(info).not.toBeNull();
    expect(info!.name).toBe('Save');
    expect(info!.type).toBe('button');
    expect(info!.selector).toContain('aria-label');
  });

  it('title 属性あり → name に使用', () => {
    const e = el('a', { title: 'Help', href: '/help' });
    const info = getMeaningfulInfo(e)!;
    expect(info.name).toBe('Help');
    expect(info.type).toBe('element');
  });

  it('data-tooltip-position あり → name に textContent を使用', () => {
    const e = el('div', { 'data-tooltip-position': 'top' });
    e.textContent = 'Open Claudian';
    const info = getMeaningfulInfo(e)!;
    expect(info.name).toBe('Open Claudian');
  });

  it('role="tab" → type=tab', () => {
    const e = el('div', { role: 'tab' });
    e.textContent = 'Settings';
    const info = getMeaningfulInfo(e)!;
    expect(info.type).toBe('tab');
    expect(info.name).toBe('Settings');
  });

  it('意味なし要素 → null', () => {
    const e = el('span', { class: 'spacer' });
    expect(getMeaningfulInfo(e)).toBeNull();
  });

  it('workspace-ribbon 祖先 → context="ribbon"', () => {
    const ribbon = el('div', { class: 'workspace-ribbon' });
    const btn = el('button', { 'aria-label': 'X' });
    ribbon.appendChild(btn);
    document.body.appendChild(ribbon);
    const info = getMeaningfulInfo(btn)!;
    expect(info.context).toBe('ribbon');
    document.body.removeChild(ribbon);
  });

  it('modal 祖先 → context="modal"', () => {
    const modal = el('div', { class: 'modal' });
    const btn = el('button', { 'aria-label': 'OK' });
    modal.appendChild(btn);
    document.body.appendChild(modal);
    const info = getMeaningfulInfo(btn)!;
    expect(info.context).toBe('modal');
    document.body.removeChild(modal);
  });

  it('path は祖先を > で連結 (最大 5 階層)', () => {
    const root = el('div');
    root.classList.add('workspace-ribbon');
    const a = el('div'); a.classList.add('a');
    const b = el('div'); b.classList.add('b');
    const c = el('div'); c.classList.add('c');
    const d = el('div'); d.classList.add('d');
    const e = el('div'); e.classList.add('e');
    const btn = el('button', { 'aria-label': 'X' });
    root.appendChild(a); a.appendChild(b); b.appendChild(c); c.appendChild(d); d.appendChild(e); e.appendChild(btn);
    document.body.appendChild(root);
    const info = getMeaningfulInfo(btn)!;
    expect(info.path.split(' > ').length).toBeLessThanOrEqual(5);
    document.body.removeChild(root);
  });
});
```

- [ ] **Step 2: テスト実行 → FAIL 確認**

```bash
npm test -- tests/features/object/inspector.test.ts
```

Expected: `Cannot find module '../../../src/features/object/inspector'` で全失敗。

- [ ] **Step 3: ObjectInspector を実装**

`src/features/object/inspector.ts`:

```typescript
export type ObjectInfo = {
  name: string;
  type: string;
  selector: string;
  path: string;
  context: string;
  attributes?: Record<string, string>;
};

const CONTEXT_SELECTORS: Array<[string, string]> = [
  ['.workspace-ribbon', 'ribbon'],
  ['.workspace-sidedock', 'sidebar'],
  ['.modal', 'modal'],
  ['.setting-item', 'settings'],
  ['.menu', 'menu'],
  ['.workspace', 'workspace'],
];

function getTag(el: HTMLElement): string {
  return el.tagName.toLowerCase();
}

function getMeaningfulSource(el: HTMLElement): { name: string; sourceAttr: string } | null {
  // 優先順位: aria-label > data-tooltip-position > title > role+textContent > id+textContent > placeholder > textContent
  const aria = el.getAttribute('aria-label');
  if (aria) return { name: aria, sourceAttr: 'aria-label' };

  if (el.hasAttribute('data-tooltip-position')) {
    return { name: el.textContent?.trim() || '', sourceAttr: 'data-tooltip-position' };
  }

  const title = el.getAttribute('title');
  if (title) return { name: title, sourceAttr: 'title' };

  const role = el.getAttribute('role');
  if (role && /^(button|menuitem|tab|switch|slider|link|checkbox|radio|option)$/.test(role)) {
    return { name: el.textContent?.trim() || role, sourceAttr: 'role' };
  }

  const id = el.getAttribute('id');
  if (id && document.querySelectorAll(`#${CSS.escape(id)}`).length === 1) {
    return { name: id, sourceAttr: 'id' };
  }

  const placeholder = el.getAttribute('placeholder');
  if (placeholder) return { name: placeholder, sourceAttr: 'placeholder' };

  return null;
}

function inferContext(el: HTMLElement): string {
  let cur: HTMLElement | null = el;
  while (cur) {
    for (const [sel, ctx] of CONTEXT_SELECTORS) {
      if (cur.matches(sel)) return ctx;
    }
    cur = cur.parentElement;
  }
  return 'unknown';
}

function buildPath(el: HTMLElement): string {
  const parts: string[] = [];
  let cur: HTMLElement | null = el;
  while (cur && parts.length < 5) {
    const cls = cur.className && typeof cur.className === 'string' ? `.${cur.className.trim().split(/\s+/).join('.')}` : '';
    parts.unshift(`${cur.tagName.toLowerCase()}${cls}`);
    cur = cur.parentElement;
  }
  return parts.join(' > ');
}

function escapeSelector(value: string): string {
  return value.replace(/["\\]/g, '\\$&');
}

function getUniqueSelector(el: HTMLElement): string {
  const aria = el.getAttribute('aria-label');
  if (aria) return `${getTag(el)}[aria-label="${escapeSelector(aria)}"]`;
  if (el.id) return `#${CSS.escape(el.id)}`;
  if (el.className && typeof el.className === 'string') {
    const cls = el.className.trim().split(/\s+/)[0];
    if (cls) return `${getTag(el)}.${cls}`;
  }
  return getTag(el);
}

export function getMeaningfulInfo(el: HTMLElement | null | undefined): ObjectInfo | null {
  if (!el || !(el instanceof HTMLElement)) return null;
  const src = getMeaningfulSource(el);
  if (!src) return null;

  const tag = getTag(el);
  const role = el.getAttribute('role');
  const type = role || (tag === 'a' ? 'link' : tag === 'button' ? 'button' : tag === 'input' ? 'input' : 'element');

  return {
    name: src.name,
    type,
    selector: getUniqueSelector(el),
    path: buildPath(el),
    context: inferContext(el),
    attributes: src.sourceAttr ? { [src.sourceAttr]: src.name } : undefined,
  };
}
```

- [ ] **Step 4: テスト実行 → PASS 確認**

```bash
npm test -- tests/features/object/inspector.test.ts
```

Expected: 8 件 all PASS。

- [ ] **Step 5: Commit**

```bash
git add src/features/object/inspector.ts tests/features/object/inspector.test.ts
git commit -m "feat(object): add ObjectInspector with meaningful element detection"
```

---

### Task 3: ObjectFormatter 実装 (TDD)

**Files:**
- Create: `src/features/object/formatter.ts`
- Create: `tests/features/object/formatter.test.ts`

**Interfaces:**
- Consumes: `ObjectInfo`
- Produces: `string`（構造化マークダウン）

- [ ] **Step 1: 失敗するテストを書く**

`tests/features/object/formatter.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { formatObject } from '../../../src/features/object/formatter';
import type { ObjectInfo } from '../../../src/features/object/inspector';

const sample: ObjectInfo = {
  name: 'Open Claudian',
  type: 'button',
  selector: ".ribbon-item[aria-label='Open Claudian']",
  path: '.workspace-ribbon > div.ribbon-item-group > .ribbon-item',
  context: 'ribbon',
};

describe('formatObject', () => {
  it('@object[type] ヘッダを含む', () => {
    const out = formatObject(sample);
    expect(out).toMatch(/^@object\[button\]/m);
  });

  it('name, type, context, selector, path をすべて含む', () => {
    const out = formatObject(sample);
    expect(out).toContain('name: Open Claudian');
    expect(out).toContain('type: button');
    expect(out).toContain('context: ribbon');
    expect(out).toContain("selector: .ribbon-item[aria-label='Open Claudian']");
    expect(out).toContain('path: .workspace-ribbon');
  });

  it('context が unknown の場合は省略', () => {
    const out = formatObject({ ...sample, context: 'unknown' });
    expect(out).not.toContain('context:');
  });

  it('attributes がある場合は末尾に追加', () => {
    const out = formatObject({ ...sample, attributes: { 'aria-label': 'Open Claudian' } });
    expect(out).toMatch(/attributes:.*aria-label/m);
  });
});
```

- [ ] **Step 2: テスト実行 → FAIL 確認**

```bash
npm test -- tests/features/object/formatter.test.ts
```

Expected: module not found エラー。

- [ ] **Step 3: ObjectFormatter を実装**

`src/features/object/formatter.ts`:

```typescript
import type { ObjectInfo } from './inspector';

export function formatObject(info: ObjectInfo): string {
  const lines: string[] = [];
  lines.push(`@object[${info.type}]`);
  lines.push(`name: ${info.name}`);
  lines.push(`type: ${info.type}`);
  if (info.context && info.context !== 'unknown') {
    lines.push(`context: ${info.context}`);
  }
  lines.push(`selector: ${info.selector}`);
  lines.push(`path: ${info.path}`);
  if (info.attributes && Object.keys(info.attributes).length > 0) {
    const attrs = Object.entries(info.attributes)
      .map(([k, v]) => `${k}=${JSON.stringify(v)}`)
      .join(', ');
    lines.push(`attributes: ${attrs}`);
  }
  return lines.join('\n');
}
```

- [ ] **Step 4: テスト実行 → PASS 確認**

```bash
npm test -- tests/features/object/formatter.test.ts
```

Expected: 4 件 all PASS。

- [ ] **Step 5: Commit**

```bash
git add src/features/object/formatter.ts tests/features/object/formatter.test.ts
git commit -m "feat(object): add ObjectFormatter (ObjectInfo -> structured markdown)"
```

---

### Task 4: ObjectContextMenu 実装

**Files:**
- Create: `src/features/object/context-menu.ts`
- Create: `src/features/object/index.ts`
- Create: `tests/features/object/context-menu.test.ts` (predicate 単体テスト)

**Interfaces:**
- Consumes: `App` / `MenuStore` / `excludeSelectors: string[]`
- Produces: `registerObjectContextMenu(plugin, store)` で contextmenu listener 登録

- [ ] **Step 1: predicate の失敗テストを書く**

`tests/features/object/context-menu.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { isExcluded } from '../../../src/features/object/context-menu';

describe('isExcluded', () => {
  it('除外セレクタにマッチすれば true', () => {
    const div = document.createElement('div');
    div.classList.add('cb-popup');
    document.body.appendChild(div);
    expect(isExcluded(div, ['.cb-popup'])).toBe(true);
    document.body.removeChild(div);
  });

  it('マッチしなければ false', () => {
    const btn = document.createElement('button');
    btn.classList.add('foo');
    document.body.appendChild(btn);
    expect(isExcluded(btn, ['.cb-popup', '.menu'])).toBe(false);
    document.body.removeChild(btn);
  });

  it('複数の除外セレクタのいずれかにマッチすれば true', () => {
    const div = document.createElement('div');
    div.classList.add('menu');
    document.body.appendChild(div);
    expect(isExcluded(div, ['.cb-popup', '.menu'])).toBe(true);
    document.body.removeChild(div);
  });

  it('空の除外リストは常に false', () => {
    const div = document.createElement('div');
    document.body.appendChild(div);
    expect(isExcluded(div, [])).toBe(false);
    document.body.removeChild(div);
  });
});
```

- [ ] **Step 2: テスト実行 → FAIL 確認**

```bash
npm test -- tests/features/object/context-menu.test.ts
```

Expected: module not found エラー。

- [ ] **Step 3: context-menu.ts を実装**

`src/features/object/context-menu.ts`:

```typescript
import { Menu, type App, type Plugin } from 'obsidian';
import { getMeaningfulInfo, type ObjectInfo } from './inspector';
import { formatObject } from './formatter';
import { addTextToClaudian } from '../selection/core';

export function isExcluded(el: HTMLElement, excludeSelectors: string[]): boolean {
  if (!excludeSelectors || excludeSelectors.length === 0) return false;
  return excludeSelectors.some((sel) => {
    try {
      return el.closest(sel) !== null;
    } catch {
      return false;
    }
  });
}

export interface ObjectMenuStore {
  load(): {
    selection: {
      objectMenuEnabled: boolean;
      objectMenuExcludeSelectors: string[];
    };
  };
}

export function registerObjectContextMenu(plugin: Plugin, store: ObjectMenuStore): void {
  plugin.registerDomEvent(document, 'contextmenu', (evt: MouseEvent) => {
    const settings = store.load().selection;
    if (!settings.objectMenuEnabled) return;

    const target = evt.target;
    if (!(target instanceof HTMLElement)) return;
    if (isExcluded(target, settings.objectMenuExcludeSelectors)) return;

    const info = getMeaningfulInfo(target);
    if (!info) return; // 意味なし要素 → 何もしない (既存メニュー表示)

    const menu = new Menu();
    menu.addItem((item) =>
      item
        .setTitle('Add to Claudian')
        .setIcon('message-square-plus')
        .onClick(async () => {
          const markdown = formatObject(info);
          await addTextToClaudian((plugin as unknown as { app: App }).app, markdown);
        })
    );
    menu.showAtPosition({ x: evt.clientX, y: evt.clientY });
    evt.preventDefault();
  });
}
```

- [ ] **Step 4: index.ts を実装**

`src/features/object/index.ts`:

```typescript
export { getMeaningfulInfo, type ObjectInfo } from './inspector';
export { formatObject } from './formatter';
export { registerObjectContextMenu, isExcluded } from './context-menu';
```

- [ ] **Step 5: テスト実行 → PASS 確認**

```bash
npm test -- tests/features/object/
```

Expected: 4 件 all PASS。

- [ ] **Step 6: Commit**

```bash
git add src/features/object/ tests/features/object/
git commit -m "feat(object): add ObjectContextMenu with Obsidian Menu integration"
```

---

### Task 5: 設定 UI 統合

**Files:**
- Modify: `src/settings/SettingTabSelection.ts`

**Interfaces:**
- Consumes: 既存 `SettingTabSelection` クラス
- Produces: 新サブセクション「Object context menu」(toggle + exclude selectors list)

- [ ] **Step 1: 既存実装を確認**

```bash
cat src/settings/SettingTabSelection.ts
```

期待: `delayMs` の下に新しい Setting を追加する。

- [ ] **Step 2: サブセクション見出しを追加**

`src/settings/SettingTabSelection.ts` の `display()` メソッド末尾、`delayMs` Setting の後に挿入:

```typescript
// === v0.2.0: Object context menu ===
containerEl.createEl('h3', { text: 'Object context menu' });
containerEl.createEl('p', {
  text: 'UI 要素を右クリック → "Add to Claudian" を context menu に追加します。',
  attr: { style: 'color: var(--text-muted); font-size: 0.85em;' },
});

new Setting(containerEl)
  .setName('Enable object context menu')
  .setDesc('ON で右クリックメニュー機能を有効化')
  .addToggle((t) =>
    t.setValue(settings.selection.objectMenuEnabled).onChange(async (v) => {
      settings.selection.objectMenuEnabled = v;
      await this.save();
    })
  );

// 除外セレクタの追加 UI
let excludeInput = '';
new Setting(containerEl)
  .setName('Exclude selectors')
  .setDesc('CSS セレクタ (例: .menu) を追加すると、その要素内ではメニューを表示しません')
  .addText((t) => {
    t.setPlaceholder('.my-selector');
    t.onChange((v) => { excludeInput = v; });
    t.inputEl.addEventListener('keydown', async (e) => {
      if (e.key === 'Enter' && excludeInput.trim()) {
        const sel = excludeInput.trim();
        if (!settings.selection.objectMenuExcludeSelectors.includes(sel)) {
          settings.selection.objectMenuExcludeSelectors.push(sel);
          await this.save();
          this.display();
        }
      }
    });
  });

// 現在のリスト表示（クリックで削除）
const listEl = containerEl.createDiv('cb-object-exclude-list');
for (const sel of settings.selection.objectMenuExcludeSelectors) {
  const tag = listEl.createEl('span', {
    cls: 'cb-object-exclude-tag',
    text: sel,
  });
  tag.createEl('span', {
    cls: 'cb-object-exclude-tag-remove',
    text: '×',
  }).addEventListener('click', async () => {
    settings.selection.objectMenuExcludeSelectors =
      settings.selection.objectMenuExcludeSelectors.filter((s) => s !== sel);
    await this.save();
    this.display();
  });
}
```

- [ ] **Step 3: スタイル追加（必要な場合）**

`styles.css` に追加:

```css
.cb-object-exclude-list {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 8px;
}
.cb-object-exclude-tag {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 8px;
  background: var(--background-modifier-border);
  border-radius: 4px;
  font-family: var(--font-monospace);
  font-size: 0.85em;
}
.cb-object-exclude-tag-remove {
  cursor: pointer;
  opacity: 0.7;
}
.cb-object-exclude-tag-remove:hover {
  opacity: 1;
  color: var(--text-error);
}
```

- [ ] **Step 4: typecheck + 構文確認**

```bash
cd D:\AI-Agent\ClaudianBridge
npm run typecheck
```

Expected: エラーなし。

- [ ] **Step 5: Commit**

```bash
git add src/settings/SettingTabSelection.ts styles.css
git commit -m "feat(settings): add Object context menu sub-section in tabSelection"
```

---

### Task 6: main.ts 統合

**Files:**
- Modify: `src/main.ts`

**Interfaces:**
- Consumes: 既存 `ClaudianBridgePlugin.onload()`
- Produces: `registerObjectContextMenu(this, this.store)` 呼び出し

- [ ] **Step 1: import 追加**

`src/main.ts` の import ブロックに追加:

```typescript
import { registerObjectContextMenu } from './features/object';
```

- [ ] **Step 2: onload() 内に登録**

`onload()` メソッド、selection watcher (`let s = Dt(...)`) の登録の **後** に追加:

```typescript
// === v0.2.0: Object context menu ===
registerObjectContextMenu(this, this.store);
```

- [ ] **Step 3: typecheck**

```bash
npm run typecheck
```

Expected: エラーなし。

- [ ] **Step 4: Commit**

```bash
git add src/main.ts
git commit -m "feat(object): wire registerObjectContextMenu into plugin onload"
```

---

### Task 7: manifest.json バージョン更新

**Files:**
- Modify: `manifest.json`

- [ ] **Step 1: バージョン更新**

```json
{
  "version": "0.2.0"
}
```

- [ ] **Step 2: Commit**

```bash
git add manifest.json
git commit -m "chore: bump version to 0.2.0"
```

---

### Task 8: ビルド + デプロイ + 手動 UAT

**Files:**
- Modify: `.obsidian/plugins/claudian-bridge/main.js` (build 成果物)
- Modify: `.obsidian/plugins/claudian-bridge/manifest.json`

- [ ] **Step 1: 全テスト実行**

```bash
cd D:\AI-Agent\ClaudianBridge
npm test
```

Expected: 既存 + 新規 すべて PASS。

- [ ] **Step 2: typecheck**

```bash
npm run typecheck
```

Expected: エラーなし。

- [ ] **Step 3: プロダクションビルド**

```bash
npm run build
```

Expected: `main.js` 生成、エラーなし。

- [ ] **Step 4: デプロイ**

```bash
npm run deploy
```

Expected: `.obsidian/plugins/claudian-bridge/main.js` と `manifest.json` が更新される。

- [ ] **Step 5: Obsidian 再起動**

Obsidian を再起動（`Ctrl+R` リロード）。

- [ ] **Step 6: 手動 UAT 実行**

| # | 確認項目 | 期待 | 結果 |
|---|---------|------|:----:|
| U1 | リボン「Open Claudian」アイコン右クリック | 「Add to Claudian」表示 | ⬜ |
| U2 | クリック → Claudian 入力欄確認 | `@object[button] name: Open Claudian ...` 挿入 | ⬜ |
| U3 | 設定 OFF → アイコン右クリック | 項目表示されない | ⬜ |
| U4 | 除外セレクタ `.menu` 追加 → メニュー要素右クリック | 項目表示されない | ⬜ |
| U5 | テキスト選択 popup との同時利用 | 干渉なし | ⬜ |
| U6 | ファイル menu「Add to Claudian」と重複 | しない | ⬜ |

- [ ] **Step 7: Commit (deploy 記録)**

```bash
git add manifest.json main.js
git commit -m "release: v0.2.0 deploy (object context menu)"
```

---

## Self-Review Checklist

- [x] 仕様カバレッジ: §3.2 判定 → Task 2 / §3.3 context → Task 2 / §4.1 ObjectInfo → Task 2 / §4.2 出力 → Task 3 / §5.1 設定スキーマ → Task 1 / §5.2 設定 UI → Task 5 / §3.1 統合 → Task 6 / 7 / 8
- [x] プレースホルダースキャン: TBD / TODO / "implement later" なし
- [x] 型整合性: `ObjectInfo` (Task 2) → `formatObject(ObjectInfo)` (Task 3) → `registerObjectContextMenu` (Task 4) で一貫
- [x] グローバル制約: minAppVersion 1.7.2 / TS strict / vitest / esbuild / Obsidian Menu API / 既存パターン踏襲

---

*🛠️ Claudian Bridge v0.2.0 オブジェクトコンテキストメニュー実装計画 · MiuMiu 🐾 · 2026-08-11*
---
tags:
  - design
  - poc
  - poc-017
status: 🟢 安定
version: 1.0.0
created: 2026-09-12 09:06
modified: 2026-09-12 09:06
---
# 選択ポップアップ位置設定 設計仕様書（承認済み）

> 📂 パス：80_POC_Projects/POC_017_ClaudianBridge/02_設計文書/17_選択ポップアップ位置設定設計.md
> 📍 原本：`D:\AI-Agent\ClaudianBridge\docs\superpowers\specs\2026-09-06-selection-popup-position-design.md`
> 🏷️ バージョン：v1.0（2026-09-06 設計・承認済み・2026-09-07 v0.38.0 としてリリース済）

> 📝 **retroactive 取込**: 本ファイルは v2.28 規則に基づき実装完了後に Vault へ正式取り込み。原本（source repo specs）と一部表現が異なるが、技術内容は同一。

---

## 1. 背景・目的

MD テキスト選択時に表示される `Add to Claudian` / `Add to TTS` ポップアップの **表示位置**を「右上」または「下」から選択できる設定を追加する。

- 現状: `src/features/selection/popup.ts` の `positionPopup` は選択範囲の **下端** に常に表示する
- 課題: 選択テキストの直下だとページ下端で折り返したり、選択範囲が大きいと UI が押し下げられたりする
- 要望: 選択範囲の **右上** にポップアップを表示する（既定）
- 互換性: 既存ユーザーが「下」配置を好む場合に備え、設定で切替可能とする

### ゴール

| # | ゴール |
|:-:|--------|
| G1 | 新規設定 `selection.popupPosition: 'top-right' \| 'bottom'` を追加（既定 `top-right`） |
| G2 | `positionPopup` を両モードに対応させ、ビューポート端のクランプと反転ロジックを統一 |
| G3 | 設定 UI（SettingTabSelection）にドロップダウンを追加 |
| G4 | 既存テストを保持しつつ、top-right ケースのテストを追加 |

---

## 2. 変更対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `src/features/selection/popup.ts` | `positionPopup` に第3引数 `mode` を追加（`'top-right' \| 'bottom'`）、top-right 配置ロジック追加 |
| `src/core/settings.ts` | `ClaudianBridgeSettings.selection.popupPosition` 追加、`normalizeClaudianBridgeSettings` / `validateClaudianBridgeSettings` でマイグレーション＋型ガード |
| `src/features/selection/watcher.ts` | 発火時に `store.load()` で `cfgAtFire.selection.popupPosition` を再取得し `positionPopup` に渡す |
| `src/settings/SettingTabSelection.ts` | 設定画面に「🌐 ポップアップ位置」ドロップダウン追加 |
| `src/core/i18n.ts` | i18n 文字列追加（ja / en / zh 4 キー） |
| `tests/features/selection/popup.test.ts` | top-right ケース追加・既存テストの `mode='bottom'` 明示 |
| `tests/features/selection/watcher.test.ts` | 新既定値の検証・既存テストの `popupPosition='bottom'` 明示 |

### 影響を受けないもの

- `buildPopup`（DOM 構造は据え置き）
- `core.ts` の `addTextToClaudian`
- `styles.css`（`position: fixed` のまま、top/left の値が変わるだけ）
- `package.json`, `manifest.json`（バージョン番号のみ更新）

---

## 3. データモデル

### 3.1 `ClaudianBridgeSettings.selection` 拡張

```typescript
// src/core/settings.ts
export type PopupPosition = 'top-right' | 'bottom';

// ClaudianBridgeSettings.selection に追加
popupPosition: PopupPosition;
```

> 📝 **実装備考**: 計画書 §3.1 は独立した `SelectionSettings` 型を想定していたが、実装では `ClaudianBridgeSettings.selection` のフィールドとして直接インライン定義（既存パターン踏襲）。

### 3.2 既定値

```typescript
// DEFAULT_CLAUDIAN_BRIDGE_SETTINGS.selection に追加
selection: {
  // ... 既存フィールド ...
  // === v0.38.0 (F-032) ===
  popupPosition: 'top-right',
  // ...
}
```

### 3.3 マイグレーション + 検証

`normalizeClaudianBridgeSettings`（既存）に 1 行追加：

```typescript
popupPosition:
  r.selection?.popupPosition === 'bottom' ? 'bottom' : 'top-right',
```

`r.selection?.popupPosition` が `'bottom'` のときだけ `bottom` を採用し、未設定・それ以外（後方互換も含む）は `'top-right'` を既定とする。

`validateClaudianBridgeSettings` に型ガード追加:

```typescript
if (cfg.selection.popupPosition !== 'top-right' && cfg.selection.popupPosition !== 'bottom')
  return 'selection.popupPosition は "top-right" または "bottom" である必要があります';
```

> ⚠️ **重要**: 計画書の盲点。`validateClaudianBridgeSettings` ガードを忘れると `store.save()` が throw して設定 UI が「保存失敗」Notice を出す。

---

## 4. 配置ロジック

### 4.1 関数シグネチャ変更

```typescript
// src/features/selection/popup.ts
export function positionPopup(
  popupEl: HTMLElement,
  rect: { left: number; top: number; right: number; bottom: number },
  mode: PopupPosition = 'top-right',
): void
```

### 4.2 計算式

| mode | 基準位置 | left | top |
|:----:|---------|------|-----|
| `'top-right'` | 選択の右上、外接 6px | `rect.right - w` | `rect.top - h - margin` |
| `'bottom'` | 選択の直下、外接 6px | `rect.left` | `rect.bottom + margin` |

### 4.3 ビューポート処理

両モード共通で `margin = 6` を保証。

**top-right モード**:

```typescript
let left = rect.right - w;
if (left + w + margin > vw) left = vw - w - margin;  // 右はみ出し → 左にクランプ
left = Math.max(margin, left);

let top = rect.top - h - margin;
if (top < margin) {
  // 上はみ出し → 選択範囲の下に反転
  top = rect.bottom + margin;
}
top = Math.max(margin, top);
```

**bottom モード**（既存挙動の維持）:

```typescript
let left = rect.left;
if (left + w + margin > vw) left = vw - w - margin;
left = Math.max(margin, left);

let top = rect.bottom + margin;
if (top + h + margin > vh) {
  // 下はみ出し → 選択範囲の上に反転
  top = rect.top - h - margin;
}
top = Math.max(margin, top);
```

### 4.4 呼び出し側（設定変更レース防止）

```typescript
// src/features/selection/watcher.ts（修正）
// === v0.38.0 (F-032): 設定値変更レース防止のため発火時点で再取得 ===
const cfgAtFire = store.load();
// ...
positionPopup(popupEl, rect, cfgAtFire.selection.popupPosition);
```

---

## 5. 設定 UI

### 5.1 SettingTabSelection.ts への追加

既存の `selectionDelayMs` 設定ブロック直後（Object context menu 見出しの直前）に dropdown を追加：

```typescript
import type { PopupPosition } from '../core/settings';

new Setting(containerEl)
  .setName(s.selectionPopupPosition)
  .setDesc(s.selectionPopupPositionDesc)
  .addDropdown((dd) => {
    dd.addOption('top-right', s.selectionPopupPositionTopRight);
    dd.addOption('bottom', s.selectionPopupPositionBottom);
    dd.setValue(cfg.selection.popupPosition);
    dd.onChange(async (value) => {
      try {
        store.save({ ...cfg, selection: { ...cfg.selection, popupPosition: value as PopupPosition } });
        new Notice(s.noticeSaved);
      } catch (e) {
        new Notice(s.noticeSaveFailed.replace('{msg}', (e as Error).message));
        draw();
      }
    });
  });
```

### 5.2 i18n キー（ja / en / zh）

| キー | 日本語 | English | 中文 |
|------|--------|---------|------|
| `selectionPopupPosition` | 🌐 ポップアップ位置 | Popup position | 弹出位置 |
| `selectionPopupPositionDesc` | テキスト選択時のポップアップ表示位置 | Position of the popup on text selection | 文本选择时弹出框的显示位置 |
| `selectionPopupPositionTopRight` | 右上 | Top right | 右上 |
| `selectionPopupPositionBottom` | 下 | Bottom | 下方 |

> 📝 **実装備考**: 計画書 §5.2 は ja / en のみ記載だが、既存パターンに倣い zh も追加（実装は 3 言語フル対応）。

### 5.3 UI 判断の根拠

- **dropdown 形式**: 2 値のトグルより `top-right` ↔ `bottom` で文字列が切り替わるほうが直感的
- **desc**: 設定画面で「現在の挙動」と「選択肢の意味」を即理解できる

---

## 6. テスト計画

### 6.1 `popup.test.ts`

| ケース | 期待値 | 種別 |
|--------|--------|:----:|
| `top-right` 既定で `rect={100,200,300,220}` w=100, h=40 → `left=200, top=154` | ✓ | 新規 |
| `top-right` で `rect.top < h + margin` → 選択範囲下（`bottom + 6`）に反転 | ✓ | 新規 |
| `top-right` で右はみ出し → 既存と同じ左クランプ | ✓ | 新規 |
| `top-right` で `margin` 未満を防止 | ✓ | 新規 |
| `mode` 既定値（`mode` 引数省略時 `'top-right'`） | ✓ | 新規 |
| `bottom` モード全ケース（引数明示） | ✓ | 既存拡張（4 件） |

### 6.2 `watcher.test.ts`

| ケース | 期待値 | 種別 |
|--------|--------|:----:|
| `popupPosition` 既定が `'top-right'` になっていること | ✓ | 新規 |
| `cfg.selection.popupPosition === 'bottom'` で `top` が `bottom + 6` になること | ✓ | 既存拡張 |
| ポップアップ内クリック → クリック伝播阻止 | ✓ | 回帰 |
| ポップアップ外 `pointerdown` → ポップアップ破棄 | ✓ | 回帰 |

### 6.3 `settings.test.ts`

| ケース | 期待値 |
|--------|--------|
| 既定値 `'top-right'` 確認 | ✓ |
| 未指定 → `'top-right'` 補完 | ✓ |
| `'bottom'` 保持 | ✓ |
| 不正値 → `'top-right'` フォールバック | ✓ |
| `validateClaudianBridgeSettings` が型違反を返す | ✓ |

---

## 7. エラーハンドリング

| ケース | 挙動 |
|--------|------|
| `popupPosition` が未知の値 | TypeScript で防止。ランタイムは normalize で `'top-right'` にフォールバック |
| 選択範囲が画面外 | `Math.max(margin, ...)` クランプで画面端に張り付く |
| popup 要素が未アペンド | `offsetWidth/Height` が 0 になるが、`margin` クランプで画面端に張り付く |
| 設定マイグレーション失敗 | `r.selection?.popupPosition` が `undefined` なら `'top-right'` |
| `validateClaudianBridgeSettings` 型違反 | `store.save()` が throw → 設定 UI で「保存失敗」Notice + `draw()` 再描画 |

---

## 8. 影響範囲チェック

- ✅ `popup.ts` の `buildPopup` には変更なし（DOM 構造据え置き）
- ✅ `watcher.ts` の `setupSelectionWatcher` は `positionPopup` 呼び出し 1 行の引数追加のみ
- ✅ `core.ts`（`addTextToClaudian`）には変更なし
- ✅ CSS（`styles.css`）に変更なし（`position: fixed` のまま、top/left の値が変わるだけ）
- ⚠️ 既存ユーザーへの **可視挙動変更**: ポップアップ位置が「下」→「右上」に変わる（設定で `bottom` に戻せる）
- 🔗 間接影響: `config-store.ts` の `validateClaudianBridgeSettings` への型ガード追加で `store.save` が動作

---

## 9. リリース計画

### 9.1 バージョン

- **v0.38.0**（マイナー追加：新規設定・既定値追加のため）
- 元の計画書では v0.36.0 だったが、main 進行に合わせ v0.38.0 に統一

### 9.2 ドキュメント更新

| ファイル | 更新内容 | 状態 |
|---------|---------|:----:|
| `data.json` | （version キーは存在しない・設定オブジェクトのみ。更新不要） | 計画書誤記訂正 |
| `CHANGELOG.md` | `[0.38.0] - 2026-09-07 — 選択ポップアップ位置設定（F-032）` 追加 | ✅ |
| `versions.json` | `0.38.0` エントリ追加 | ✅ |
| `manifest.json`（× 2） | `version: 0.38.0` | ✅ |
| `package.json` | `version: 0.38.0` | ✅ |
| `08_説明書/03_リリースノート/リリースノート.md` | `## v0.38.0（2026-09-07）— 選択ポップアップ位置設定（F-032）` 節追加 | ✅ |
| `03_開発文書/05_進捗ボード.md` | F-032 完了エントリ追加（v1.2.0） | ✅ |
| `F-number_master.md` | F-032 行追加（v2.28 retroactive） | ✅ |
| `02_設計文書/17_選択ポップアップ位置設定設計.md` | 本ファイル（v2.28 retroactive） | ✅ |

### 9.3 ブランチ戦略とマージ実績

```
main (fc890e2 = v0.37.2)
 └─ feature/f032-selection-popup-position (新規 → 削除済)
     ├─ 156dca8 feat(settings): selection.popupPosition 追加（既定 'top-right'）
     ├─ 7c93c45 feat(i18n): selectionPopupPosition* 4 キー追加（ja/en/zh）
     ├─ f25b76f feat(popup): positionPopup に mode 引数追加（top-right 既定）
     ├─ 6257f63 feat(ui): SettingTabSelection にポップアップ位置 dropdown 追加
     ├─ 4856fe8 feat(watcher): selection popupPosition を positionPopup に渡す
     └─ d2195a8 docs(release): v0.38.0 リリース準備
```

マージ方式: **fast-forward**（main は feature の直接祖先・divergence なし）。
- ローカル main: `fc890e2 → d2195a8` (10 commits FF)
- origin/main: `99730b9 → d2195a8` (push 成功)

---

## 10. 用語

| 用語 | 意味 |
|------|------|
| ポップアップ | テキスト選択時に表示される `Add to Claudian` / `Add to TTS` ボタン群（`.cb-popup`） |
| 外接 | ポップアップが選択テキストに重ならず、6px の余白を保って隣接する配置 |
| 反転 | ビューポート端でポップアップ位置を上下入れ替えること |
| クランプ | ビューポート端に押し戻すこと |
| 発火時 cfg 再取得 | イベント発火時点の `store.load()` 結果を使うこと（設定変更レース防止） |

---

## 11. 関連コミット（実装記録）

| SHA | 種別 | 内容 |
|-----|------|------|
| `156dca8` | feat(settings) | `selection.popupPosition` 追加（既定 `'top-right'`） |
| `7c93c45` | feat(i18n) | `selectionPopupPosition*` 4 キー追加（ja/en/zh） |
| `f25b76f` | feat(popup) | `positionPopup` に mode 引数追加（top-right 既定） |
| `6257f63` | feat(ui) | SettingTabSelection にポップアップ位置 dropdown 追加 |
| `4856fe8` | feat(watcher) | selection popupPosition を positionPopup に渡す |
| `d2195a8` | docs(release) | v0.38.0 リリース準備 |

---

*📐 選択ポップアップ位置設定 設計仕様書 v1.0 · POC_017 Claudian Bridge · MiuMiu 🐾 · 2026-09-07 v0.38.0 リリース*

## 📝 更新記録

| バージョン | 日付 | 変更内容 | 変更者 |
|-----------|:----:|---------|:------:|
| v1.0.0 | 2026-09-12 | 初版作成 | MiuMiu 🐾 |

---
title: "リリースノート"
type: release-notes
template_id: poc/v3.1.0/08-リリースノート
template_version: 3.5.0
version: 1.7.0
status: published
created: 2026-08-10
modified: 2026-08-30
tags: 
  - リリースノート
  - Release Notes
aliases: 
  - リリースノート
language: Japanese
applied_rules_version: 2.15.0
---

> 📂 **パス**: `08_説明書/03_リリースノート/リリースノート.md`
> 🎯 **用途**: 現在のバージョン詳細説明

---

## v0.27.3 (2026-08-30)

### 🐛 修正

- **言語 auto 判定の日本語誤判定** (`src/features/tts/lang.ts`)
  - 旧ロジックは「かな文字数 > 漢字文字数」を日本語条件にしていたため、漢字多めの通常の日本語文が `zh`（中国語音声）に誤判定されていた
  - 中国語はひらがな/カタカナを使用しないため、**かなが 1 文字でも存在すれば `ja`** を最優先判定に変更
  - かなゼロの漢字のみテキストは従来どおり `zh` / `en` 判定を維持（後方互換）

### 互換性

- 既存 `data.json` への破壊的変更なし
- 言語モード固定（`ja` / `zh` / `en`）利用者は挙動変更なし

### 🧪 テスト

- TypeScript テスト **790 件 PASS**（v0.27.2 の 788 件 + 新規 2 件）

---

## v0.27.2 (2026-08-30)

### 🐛 修正

- **TTS チャンク分割時の言語切替不具合** (`src/features/tts/core.ts` / `edge-tts-local.ts` / `lang.ts`)
  - 長文読上げ時にチャンクごとに言語 auto 判定を実行していたため、区切り方次第で英語のみ・漢字のみのチャンクが発生し、読上げ途中で音声（言語）が切り替わる不具合を修正
  - `addTextToTTS` で分割**前**の全文に対して 1 回だけ `pickLang` を実行し、全チャンクに同一の言語・音声を適用
  - `edgeCloudHttpSpeak` / `localEdgeTtsSpeak` / `webSpeechSpeak` に optional `lang` 引数を追加（未指定時は従来どおりチャンク単位の auto 判定・後方互換）
  - `lang.ts` に `TtsLang` 型をエクスポート

### 互換性

- 既存 `data.json` への破壊的変更なし
- 言語モード固定（`ja` / `zh` / `en`）利用者は挙動変更なし・`auto` 利用者は 1 回の読上げ内で言語が固定される

### 🧪 テスト

- TypeScript テスト **788 件 PASS**（v0.27.1 の 787 件 + 新規 1 件）
- 新規テスト: `tests/features/tts/core-lang-consistency.test.ts`（全文 ja 判定の長文＋英語区間で全チャンクの音声が ja 統一されることを検証）

---

## v0.27.1 (2026-08-27)

### 🐛 修正

- **Thought 読上げ除外セレクタの防御的強化** (`src/features/tts/extract-report.ts`)
  - `THINKING_BLOCK_SELECTOR` を `.claudian-thinking-block` 単体から**複数クラスの OR** に拡張
  - 追加: `.claudian-thinking-content` / `.claudian-thinking-header` / `.claudian-thinking-label` / `.claudian-thinking`
  - realclaudian v2.2.4+ の DOM 構造（thinking 内容が `claudian-thinking-block` の子クラスに置かれる）に対応
  - 既存ユーザー（`thinking: false` 既定・手動 OFF）への破壊的変更なし

### 互換性

- 既存 `data.json` への破壊的変更なし
- 影響範囲: `THINKING_BLOCK_SELECTOR` 定数のみ

### 🧪 テスト

- TypeScript テスト **787 件 PASS**（v0.27.0 の 786 件 + 新規 1 件）

---

## v0.27.0 (2026-08-20)

### 新機能

- 📦 **edge_tts 完全同梱**: `git subtree` で edge-tts v7.2.8（**LGPLv3 + MIT mixed** ライセンス・詳細は `THIRD_PARTY_NOTICES.md`）を `py/edge_tts/` にバンドル。**`pip install edge-tts` 不要**に
- 🌐 **言語モード切替**: 「Add to TTS」系と「AI 自動読上げ」系（タスク終了時自動読み上げ・AI 読上げボタン）で独立した `auto / ja / zh / en` を選択可能
  - `auto` = 従来の自動判定 / `ja`・`zh`・`en` = 毎回その言語で再生
- ☁️ **クラウド EdgeTTS（HTTPS POST プロキシ）**: `edgeCloud = { serverUrl, authToken, timeout }` で任意の外部サーバを指定可能
  - リクエスト: `{ text, voice, lang }` を POST / 認証: `Authorization: Bearer <token>`（任意）
  - 旧 `claudettsHttpSpeak`（POC_015 依存・固定パス spawn）は完全削除
- 🐧 **クロスプラットフォーム対応**: Ubuntu / Linux で `python3` 自動検出 + `SIGTERM → SIGKILL` プロセス停止
- 📂 **UI 改善**: EdgeTTS モジュール場所に 📂 ボタン（OS のファイルマネージャで開く）
- i18n ラベル 15 キー × 3 言語（ja / zh / en）追加

### 仕様変更

- **デフォルトエンジン変更**: 新規ユーザー = `edge-local` ／ 既存ユーザー（`edge`）= `edge-local` に自動マイグレーション

### ⚠️ 既知の制限

- `edge` → `edge-local` マイグレーションの**永続化（data.json 書き戻し）は未実装**（ロード時に正規化されるため動作上問題なし・将来タスクで対応）
- Web Speech エンジンは言語モード固定に未対応（従来どおり自動判定のみ）

### 互換性

- 既存 `data.json` への破壊的変更なし（新フィールドはすべて optional・未設定時は `auto` / 既定値にフォールバック）
- テスト件数: 778 → **782** (+4)

### 参照

- 設計書: [[2026-08-19-tts-engine-change-local-bundle-cloud-server-language-mode]]
- 実装計画: [[18_TTSエンジン変更実装計画]]

---

## v0.26.0 (2026-08-18)

### 新機能

- 🔍 **クイック返信ボタンの検出パターン拡張**（`features/quick-reply/recommend-detector.ts`）
  - **「案」（方案なし）表記に対応**: `案1`, `案1〜4`, `案2 が推奨` などを検出
  - **追加された推奨語彙**:
    - ja: `最優先 N` / `第一選択 N` / `優先案 N` / `優先度 N`
    - zh: `首选 N` / `优选 N`
    - en: `best option N` / `prefer option N`
  - **逆順パターン対応**: `案N が推奨` / `案N をおすすめ` のように、推奨語が番号の後ろに来る形式も検出
- 方案数カウント (`extractMaxOptionCount`) も「方案|案」の OR になり、`案1〜4` 表記をカバー

### 互換性

- 既存検出パターン（`推奨 方案N` 等）は全て後方互換
- テスト件数: 744 → **756** (+12 ケース)

---

## v0.25.0 (2026-08-18)

### 新機能

- 🔘 **クイック返信ボタン全体の ON/OFF**（`general.quickReplyEnabled`・既定 ON）
- 設定画面「一般」タブで ON/OFF 切替可能
- **OFF**: ボタンの注入を停止し、既存ボタンも即時削除
- **ON**: 既存通り（showAllOptions の動作も維持）

### 互換性

- 既存 `data.json` への破壊的変更なし（`quickReplyEnabled` 未設定は自動的に `true`）

---

## v0.24.0 (2026-08-18)

### 新機能

- 🔢 **方案ボタンを常に表示する設定**（`general.quickReplyShowAllOptions`・既定 OFF）
- 設定画面「一般」タブで ON/OFF 切替可能
- **OFF（既定）**: 既存の動的表示（応答に「方案N」がある時のみ 1️⃣〜5️⃣ を表示）
- **ON**: ✅ ❌ 1️⃣〜5️⃣ を**常に表示**（LLM 応答を待たずに手動で方案送信できる）

### 互換性

- 既存 `data.json` への破壊的変更なし（`quickReplyShowAllOptions` 未設定は自動的に `false`）

---

## v0.22.0 (2026-08-17)

### 新機能

- 📁 **「_」プレフィックスフォルダ非表示**: フォルダ名の先頭が `_` のフォルダ（例: `_テンプレート`）をファイルエクスプローラから非表示にします
- 設定画面「拡張子フィルタ」タブで ON/OFF 切替可能（既定 ON）
- 「フォルダは常に表示」より優先（`_` フォルダは常に非表示）

### 互換性

- 既存ユーザーの `data.json` への破壊的変更なし（`hideUnderscoreFolders` 未設定は自動的に `true`）

---

## v0.21.1 (2026-08-17)

### 新機能

- ⏱️ **完了時ダイアログ自動クローズ設定**: バックアップ成功後、1.5 秒後にダイアログを自動で閉じる（設定画面「一般」タブで ON/OFF 切替可能、既定 ON）
- 失敗時はダイアログを閉じず、エラーログを確認できる

### 修正

- 🔧 保存先ダイアログが開かない問題を修正（Electron `dialog.showOpenDialog` → `remote.dialog` フォールバック）

### 互換性

- 既存ユーザーの `data.json` への破壊的変更なし（`backupAutoClose` 未設定は自動的に `true`）

---

## v0.21.0 (2026-08-17)

### 新機能

- 💾 **右クリックバックアップ機能**: ファイルツリーでフォルダまたはファイルを右クリック → 「💾 バックアップ」メニュー → 保存先ダイアログ → タイムスタンプ付きでバックアップ
- 設定画面「一般」タブで ON/OFF 切替可能（既定 ON）

### 互換性

- 既存ユーザーの `data.json` への破壊的変更なし（`backupEnabled` 未設定は自動的に `true`）

---

# 📢 v0.20.0 リリースノート

> 📅 リリース日付: 2026-08-16
> 🔗 設計書: [[../../02_設計文書/2026-08-16-chroma-fs-virtual-folder-design|Chroma-fs 仮想フォルダ設計]]

## 🎯 バージョンの強み

> [!success] Chroma DB の内部構造を隠してファイルエクスプローラを整理 + ドキュメントから直接 RAG 検索
> - 📁 **`chroma_db` 内部をファイルエクスプローラから非表示**（CSS 注入・設定トグル付き）
> - 🔍 **`chroma_db` 内ドキュメント右クリック → RAG 質問モーダル** で直接検索

## ✨ 新機能

| 項目 | 内容 |
|------|------|
| 仮想フォルダ非表示 | `chroma-fs.hideInternal`（既定 ON）: `chroma_db` 内部を CSS 注入で非表示 |
| 右クリック RAG 検索 | `chroma-fs.rag`（既定 ON）: ドキュメント右クリック → RAG 質問モーダル |
| バックエンド | `word-pdf-rag query.py --json` を実行 |

### 🛟 互換性

- 既存 `data.json` への破壊的変更なし（`chroma-fs` 未設定はデフォルト補完）

### 🧪 テスト

- tts: header フォールバック + ゲート順序の追加テスト

---

# 📢 v0.19.0 リリースノート

> 📅 リリース日付: 2026-08-16
> 🔗 設計書: [[../../02_設計文書/2026-08-16-tts-auto-read-final-answer-design|自動読み上げ「最終回答のみ」改善設計]]

## 🎯 バージョンの強み
- 自動読み上げが**複数ターンタスクでも最終回答のみ**を確実に読み上げる

## ✨ 新機能
- 最終回答ゲート: ツール・思考の途中ターンでは発火しない（dedup マークも付けない）

## 🐛 修正
- 思考中に発火して途中内容を読む問題
- 読み上げに思考内容が混入する問題
- 途中ターンで dedup マークが付き最終回答が読まれない問題

---

# 📢 v0.18.0 リリースノート

> 📅 リリース日付: 2026-08-16
> 🔗 設計書: [[../../02_設計文書/2026-08-16-edge-chunkmax-settings-design|EdgeTTS チャンク上限単独設定設計]]

## 🎯 バージョンの強み

> [!success] EdgeTTS のチャンク上限を単独設定可能に（エンジン別マップ化）
> - 🔊 **EdgeTTS は 1 チャンク上限 500 字**（設定 100〜2000）に拡大 — 長文を 1 リクエストで効率的に読み上げ
> - ✂️ **チャンク上限をエンジン別に設定**（edge / webspeech / plachta の 3 スライダー）
> - 🔄 **後方互換マイグレーション** — 既存 `chunkMaxChars` は webspeech/plachta に引き継ぎ、edge は 500 に初期化

---

## ✨ 新機能

| 項目 | 内容 |
|------|------|
| エンジン別チャンク上限 | `tts.chunkMaxChars` を `{ edge: 500, webspeech: 140, plachta: 140 }` のマップに変更 |
| EdgeTTS 単独設定 | edge のみ 100〜2000（既定 500）で調整可。webspeech/plachta は 50〜140（既定 140） |
| 設定 UI | チャンク上限スライダーを 3 つ（EdgeTTS / WebSpeech / Plachta）に分割 |

### 🛟 互換性

- 既存 `chunkMaxChars`（number・v0.17）は **webspeech/plachta に引き継ぎ・edge は 500 に初期化**（マイグレーション）
- 既存の読み上げ挙動（webspeech/plachta 140 分割）は維持

### 🧪 テスト

- settings: エンジン別マップのデフォルト・マイグレーション・クランプ・validate（+2 回帰テスト）
- core: edge 既定 500 分割・plachta 個別 100 分割
- 全件 **592 passed / 1 skipped**・typecheck エラー 0

---

<details>
<summary>📜 v0.17.0（履歴）</summary>

# 📢 v0.17.0 リリースノート

> 📅 リリース日付: 2026-08-16
> 🔗 設計書: [[../../02_設計文書/2026-08-16-tts-read-spec-enhancement-design|TTS 読み上げ仕様改良設計]]

## 🎯 バージョンの強み

> [!success] TTS 読み上げ仕様を統一・拡張（MD Add to TTS / チャンク設定 / タイプ別フィルタ）
> - 📄 **MD ファイル右クリック「Add to TTS」** — ファイル本文（frontmatter・コード除く）を右クリックで読み上げ
> - ✂️ **チャンク上限を全エンジン共通化**（既定 140 字・設定で 50〜140 調整可）
> - 🎛️ **読み上げ内容フィルタをタイプ別に個別設定**（4 タイプ × 8 項目の表形式チェック）
> - 🔀 **全エントリポイントを共通 `speakText` に統一**（空テキスト「入力がありません」・失敗時エラー Notice を一元化）

---

## ✨ 新機能

| 項目 | 内容 |
|------|------|
| MD Add to TTS | ファイルエクスプローラの `.md` 右クリック → 「Add to TTS」で本文読み上げ |
| チャンク上限設定 | `tts.chunkMaxChars`（50〜140・既定 140）。edge/webspeech も 140 で分割 |
| タイプ別フィルタ | `tts.speechFilter.{selection,autoRead,message,inputAi}` × 8 項目（絵文字/顔文字/ASCII/短コード/コールアウト/テーブル/コード/思考）。チェック=読む |
| 仕様統一 | 空テキスト「入力がありません」・読み上げ失敗エラー Notice を全タイプ共通化 |

### 🛟 互換性

- 既存 `cli.speech_filter`（ON=除去）を全タイプの初期値へ**反転マッピング**（後方互換マイグレーション）
- 既存 `excludeCallouts` を `callout` フィルタへ反転引き継ぎ
- ③📖全文トグル・⑥CLI（voice-config）は対象外（既存のまま）

### 🧪 テスト

- 追加テスト: settings（chunkMaxChars/speechFilter マイグレーション 6 件）・speech-filter（6 件）・extract-report（4 件）・speak（8 件）・md-file-read（5 件）+ 既存テスト更新
- 全件 **581 passed / 1 skipped**・typecheck エラー 0

---

</details>

<details>
<summary>📜 v0.16.0（履歴）</summary>

# 📢 v0.16.0 リリースノート

> 📅 リリース日付: 2026-08-16
> 🔗 設計書: [[../../02_設計文書/2026-08-16-input-ai-read-button-design|AI読み上げボタン設計]]

## 🎯 バージョンの強み

> [!success] AI読み上げボタン（✨）— 入力文をAIが指令文に整形して読み上げ
> - ✨ ClaudianChat 入力ツールバー右端に ✨ ボタンを追加
> - 🤖 Claude Code CLI（`claude -p`）で入力文の意図を解釈し、分かりやすい指令文に整形
> - 📝 整形文で入力欄を上書き（元文は Notice に表示・コピペで復元可能）
> - 🔊 読み上げは既存 TTS エンジン設定（edge / webspeech / plachta）に従う
> - 🛟 CLI 失敗時は元文を変更せずそのまま読み上げるフォールバック付き

---

## ✨ 新機能

| 項目 | 内容 |
|------|------|
| AI読み上げボタン | 入力文 → AI 整形 → 入力欄上書き → TTS 読み上げを ✨ ワンクリックで実行 |
| 整形 LLM | Claude Code CLI（プロンプトは stdin 渡し・30 秒タイムアウト・入力と同一言語を維持） |
| 無効化設定 | ClaudianBridge 設定の TTS タブ「AI読み上げボタン」トグル（既定 ON）で非表示化 |
| 失敗フォールバック | CLI 起動不可・タイムアウト・空応答時は元文を保持して元文を読み上げ |

---

</details>

<details>
<summary>📜 v0.15.0（履歴）</summary>

# 📢 v0.15.0 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] コールアウトを読み上げ対象から除外（設定で ON/OFF）
> - ✨ `> [!type]` 形式のコールアウト（成功・注意等）を読み上げから除外（既定 ON）
> - 🎛️ ClaudianBridge 設定画面（TTS タブ）で切替可能
> - 🔗 自動読み上げ（full/header）・メッセージ読上げボタンの全経路に適用

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| コールアウト除外設定 | `tts.excludeCallouts`（既定 true）を追加。`.callout` を読み上げから除外 |
| 設定 UI | TTS タブに「コールアウトを読み上げ対象から除外」トグルを追加 |

---

</details>

<details>
<summary>📜 v0.14.4（履歴）</summary>

# 📢 v0.14.4 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 読み上げからコードブロックを除外
> - 🐛 言語ラベル（`bash` 等）やコード本文が読み上げに混入する問題を修正
> - 🛡️ `EXCLUDED_FROM_SPEECH` に `.claudian-code-wrapper` を追加（full / header / 読上げボタン全経路）

---

## 🐛 修正

| 項目 | 内容 |
|------|------|
| コードブロック除外 | `.claudian-code-wrapper`（言語ラベル + `pre` 本文）を読み上げテキストから除外 |

---

</details>

<details>
<summary>📜 v0.14.3（履歴）</summary>

# 📢 v0.14.3 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ヘッダーの「まとめ」マーカーに ✅ を追加
> - ✨ ✅ で始まる blockquote も 📢 と同様に**まとめとして読み上げ**
> - 🧠 判定: `isSummaryMarker()`（📢 / ✅ で始まる）

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| ✅ マーカー対応 | ヘッダースコープで ✅ blockquote を 📢 と同等に扱う（例: `> ✅ ビルド・コミット状況`） |

---

</details>

<details>
<summary>📜 v0.14.2（履歴）</summary>

# 📢 v0.14.2 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ヘッダースコープで「一項目のみ」の応答も読み上げ対象に
> - ✨ 見出しが1つ・導入文の無い応答（例: `## ビルド・コミット状況`）も**その節を読み上げ**
> - 🛡️ データ表（table）はヘッダーでは読まない（まとめのみ）
> - 🧠 読み上げ優先順: 📢 報告 → 導入文 → 一項目のみの節

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| 一項目のみ対応 | 見出し1つ・導入文なしの応答は、見出し＋本文（テーブル除く）を「まとめ」として読む |
| 読み上げ優先順 | ① 📢 blockquote ② 最初の見出しまでの導入文 ③ 一項目のみの節 → どれも無ければ読まない |

---

</details>

<details>
<summary>📜 v0.14.1（履歴）</summary>

# 📢 v0.14.1 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ヘッダースコープの読み上げ対象を見直し（結果全体まとめのみ）
> - ✨ 📢 が無い応答でも**最初の見出しまでの導入文**を「まとめ」として読み上げ
> - 🛡️ 📢・見出しが無い応答は読まない（レンダリング中のプレースホルダ誤読を防止）
> - 🧠 Thinking ブロック・詳細・次のアクションはヘッダーでは読まない（全文のみ）

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| ヘッダー読み上げ対象 | 📢 blockquote → その報告 / 📢 なし → 最初の見出し（h1-h6）までの導入文 / どちらも無し → 読まない |
| 全文との関係 | ヘッダーは「結果全体まとめ」のみ。詳細・次のアクションは全文モードのみ読み上げ |

---

</details>

<details>
<summary>📜 v0.14.0（履歴）</summary>

# 📢 v0.14.0 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] メッセージ読上げボタンを追加
> - ✨ ClaudianChat 結果欄の各テキストブロックに**読上げボタン**を追加（コピーボタンの左隣に配置）
> - 🖱️ クリックで当該ブロックの可視テキストを `addTextToTTS` 経由で読み上げ
> - 🎯 speech_filter・ミュート連動は既存の読み上げ経路をそのまま踏襲

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| 読上げボタン追加 | ClaudianChat 結果欄の各テキストブロック（コピーボタンと同じ範囲）に読上げボタンを追加 |
| 読み上げ経路 | クリックで当該ブロックの可視テキストを `addTextToTTS` 経由で読み上げ。speech_filter・ミュート連動は既存踏襲 |

---

</details>

<details>
<summary>📜 v0.13.1（履歴）</summary>

# 📢 v0.13.1 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] full 読み上げで思考ブロック（Thought for Xs）を発話から除外
> - 🐛 `full` モードで realclaudian の **Extended thinking ブロック**（`.claudian-thinking-block`）が読み上げに混入
> - 🛡️ `readVisibleTextExcluding()` を追加し、思考ラベル「Thought for 1s」や思考本文を発話から除外
> - ✅ 実ブラウザは innerText + display:none、テスト（jsdom）は clone 除去で両対応

---

## 🐛 修正

| 項目 | 内容 |
|------|------|
| 思考ブロック除外 | `full` 読み上げ時、`.claudian-thinking-block`（Thought for Xs ラベル・思考本文）を読み上げテキストから除外 |
| テスト追加 | `full scope: 思考ブロック（Thought for）を読み上げに含めない`（全 439 PASS） |

---

</details>

<details>
<summary>📜 v0.13.0（履歴）</summary>

# 📢 v0.13.0 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 読み上げ経路をプラグインに統一（全応答）+ ミュートボタンが全読み上げを反映
> - ✨ 自動読み上げを「📢 報告のみ」→「全応答」に拡張（scope=full 時）
> - 🛡️ Claude Code CLI Stop hook 由来の読み上げを廃止し、プラグイン一元化
> - 🐛 読み上げ中にミュートアイコンが点滅しない問題を根因から解消

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| 全応答読み上げ | `autoRead.scope=full` で 📢 有無に関わらず最後の応答を全文読み上げ（CLI Stop hook に代わる一元化） |
| ミュート連動 | 全読み上げが再生レジストリ経由になるため、ミュートボタンの点滅・停止が機能 |

---

## 🗑️ 廃止

| 項目 | 内容 |
|------|------|
| CLI Stop hook 読み上げ | `~/.claude/settings.json` の `hooks.Stop`（tts-speak.py）を無効化。Claude Code CLI の全応答読み上げは Claudian Bridge に移管 |

---

</details>

<details>
<summary>📜 v0.12.6（履歴）</summary>

# 📢 v0.12.6 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ミュートボタンで確実に音声を停止
> - 🐛 読み上げ中にミュートボタンで音声が停止しない問題をさらに強化
> - 🛡️ クリック時に常に停止を試行 + edge 子プロセス PID を直接追跡して確実に kill

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-015 | 読み上げ中にミュートボタンで音声が停止しない（再生検知が不確実なケース） | ✅ 修正（クリック時は常に `stopAllPlayback()` を実行 + edge 子プロセス PID 直接追跡で `taskkill /T` 停止） |

---

## 🔧 改善

| 項目 | 内容 |
|------|------|
| 停止の堅牢化 | レジストリ追跡が外れても、edge 子プロセス PID を直接保持して確実に停止 |
| 診断ログ | 再生検知・ステータス表示の `[cb-tts]` ログを追加（デバッグ用） |

---

</details>

<details>
<summary>📜 v0.12.5（履歴）</summary>

# 📢 v0.12.5 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ツールバーボタンの横幅をアイコンサイズに最適化
> - ✨ `min-width: 5em` → `2em` + padding に調整し、アイコンのみ表示に合わせて横幅を最小化

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| ボタン横幅最適化 | アイコンのみ表示に合わせ、ボタンの最小幅を `2em`・padding `0.35em` に調整 |

---

</details>

<details>
<summary>📜 v0.12.4（履歴）</summary>

# 📢 v0.12.4 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ツールバーボタンをアイコンのみ表示に最小化
> - ✨ ミュート・全文ボタンをアイコンのみ（🔊/⏹/🔇/📖/📄）表示に変更
> - 💡 文字ラベルは tooltip（ホバー表示）に移動

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| ボタン表示最小化 | ミュート/全文ボタンをアイコンのみ表示に変更し、サイズを最小化。説明は tooltip で表示 |

---

</details>

<details>
<summary>📜 v0.12.3（履歴）</summary>

# 📢 v0.12.3 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ミュートボタンの点滅・停止を機能させる + ボタン表示を最小化
> - 🐛 読み上げ中にミュートボタンが点滅しない問題を修正
> - 🐛 読み上げ中にミュートボタンで音声が停止しない問題を修正
> - ✨ ボタンラベルを最小化（ミュートは全状態で「ミュート」表示）

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-013 | 読み上げ中にミュートボタンが点滅しない（edge エンジンで音声再生が別プロセス実行のため再生レジストリが「再生中」を検知できず） | ✅ 修正（`CrossPlatformPlayer` を音声終了まで待機する `subprocess.run` に変更） |
| KB-014 | 読み上げ中にミュートボタンで音声が停止しない（PowerShell プレイヤーが別プロセスのため） | ✅ 修正（edge 停止ハンドラを `taskkill /T` のプロセスツリー kill に変更） |

---

## ✨ 改善

| 項目 | 内容 |
|------|------|
| ボタン表示最小化 | ミュートボタンは全状態で「ミュート」（🔊/⏹/🔇）表記に短縮し、サイズを削減 |

---

</details>

<details>
<summary>📜 v0.12.2（履歴）</summary>

# 📢 v0.12.2 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 読み上げ文最適化（speech_filter）を全経路に適用
> - ✨ emoji / 顔文字 / ASCII 表情 / emoji 短コード を除去して読み上げ品質を改善
> - 🐛 設定では定義・UI 表示・CLI 同期されていたが、実際の読み上げに適用されていなかった問題を修正
> - 🔧 CLI 側（claude-tts スキル）にも POC_015 由来の speech_filter 実装を復元

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| `filterSpeechText` | 読み上げ文最適化関数。emoji（Unicode ブロック）・顔文字（括弧内特徴文字 2 つ以上）・ASCII 表情・emoji 短コード を除去 |
| 全経路適用 | チャット自動読み上げ・手動「Add to TTS」は `tts.cli.speech_filter` を参照。CLI stop_hook は voice-config の `speech_filter` を参照 |

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-011 | speech_filter 設定が定義・UI 表示・CLI 同期のみで、実際の読み上げに適用されていなかった | ✅ 修正（全読み上げ経路で適用） |
| KB-012 | claude-tts スキルの extractor に speech_filter が移行時に欠落していた | ✅ 修正（POC_015 実装を復元） |

---

</details>

<details>
<summary>📜 v0.12.1（履歴）</summary>

# 📢 v0.12.1 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 自動読み上げの誤報修正 + 複数タブ対応
> - 🐛 📢 報告の無い通常応答でも「⚠️ 📢 検出不可」通知が毎回表示される問題を修正
> - ✨ 複数タブ表示時もストリーミング完了タブの 📢 報告を正しく抽出
> - ⏱️ stream-end 直後のレンダリング遅延に備えた抽出リトライを導入

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-008 | 📢 報告の無い通常応答でも「⚠️ 自動読み上げ: 📢 検出不可」通知が毎回表示される（読み上げ失敗と誤認される） | ✅ 修正（📢 なしは静かにスキップ） |
| KB-009 | 複数タブ表示時に `.claudian-messages` の取得が最初のタブに固定され、ストリーミング完了タブの 📢 報告を抽出できない | ✅ 修正（アクティブタブ優先） |

---

## 🔧 最適化と改善

| 最適化項目 | 説明 |
|--------|------|
| 抽出リトライ | stream-end 直後は markdown レンダリング未完了のことがあるため、400ms × 最大5回リトライして 📢 blockquote を検出 |
| 診断ログ整理 | auto-read のイベント毎ログを `console.debug` に格下げ（成功時 🔊 通知は維持） |

---

</details>

<details>
<summary>📜 v0.12.0（履歴）</summary>

# 📢 v0.12.0 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] ツールバー操作の統合・復活
> - 🔊 ミュートボタンを復活（旧 claude-tts-settings の 3 状態モデル）
> - 📖 全文ボタンと自動読み上げ範囲を統一同期（どこから変更しても相互反映）
> - ボタンをアイコン＋テキスト化し、再生中は点滅表示

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| ミュートボタン復活 | `toolbar-buttons.ts` に 3 状態ミュートを統合。🔊(非再生) / 🔊点滅(再生中・クリック=停止) / 🔇(ミュート)。`tts.enabled` をトグル |
| 再生停止基盤 | `playback-registry.ts` 新設。edge(child.kill) / webspeech(synth.cancel) / plachta(audio.pause) に停止ハンドルを統合 |
| 全文⇄範囲の統一同期 | 📖 ボタンと「タスク終了時自動読み上げ範囲」を `withFullTextState` で同期（`autoRead.scope` ⟺ `cli.full_text`） |
| ボタン表示改善 | アイコン＋テキスト（ja/en/zh）・再生中は点滅アニメーション |

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-006 | edge エンジンで意図的停止後に error イベントが到達するとエラー Notice を表示する | ✅ 修正（intentionalStop ガード追加） |
| KB-007 | v0.11.1 の旧📖ボタン ON（`full_text=true`）が新ボタン表示と不整合 | ✅ 修正（`autoRead.scope=full` に引き継ぎ・旧設定尊重） |

---

</details>

<details>
<summary>📜 v0.11.1（履歴）</summary>

# 📢 v0.11.1 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 不具合修正 + ボタン後継
> - 🐛 自動読み上げが発火しない問題を修正（hook 先を realclaudian 実測構造の `view.getTabManager().callbacks` に変更）
> - ✨ チャット入力ツールバーに「📖 全文読み上げ」トグルボタンを追加（旧 claude-tts-settings の後継）

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| ツールバー全文ボタン | `toolbar-fulltext-button.ts` 新規。入力ツールバー末尾に 📖/📄 ボタンを注入し `tts.cli.full_text` をトグル（保存時 voice-config.json へ反映）。二重注入防止属性 `data-cb-fulltext` |
| 旧プラグイン無効化 | `disable-legacy` の対象に `claude-tts-settings` を追加（ボタン二重注入と設定非同期の解消） |

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-004 | 応答完了時に 📢 報告が自動読み上げされない — view 直下の `callbacks`/`onStreamingChanged` は実在せず、実測では `view.getTabManager().callbacks` にあったため hook が空振ち | ✅ 修正（tabManager 経由に変更 + tabId 単位の遷移判定） |
| KB-005 | チャット画面の「全文読み上げ」ボタン（旧 claude-tts-settings 製）と Bridge 設定が非同期 | ✅ 解消（Bridge 製ボタンに置換 + 旧プラグイン無効化） |

---

</details>

<details>
<summary>📜 v0.11.0（履歴）</summary>

# 📢 v0.11.0 リリースノート

> 📅 リリース日付: 2026-08-15

## 🎯 バージョンの強み

> [!success] 新機能
> - ✨ タスク終了時の自動 TTS 読み上げ — Claudian チャットで 📢 終了報告を検出し、自動で音声読み上げ
> - ✨ 読み上げ範囲選択（📢 ヘッダーのみ / メッセージ全文）を設定タブで切り替え可能
> - ✨ latest-wins コーディネータ — 読み上げ中に新しい報告が来たら最新を優先

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| `tts.autoRead` | タスク終了時の自動読み上げ設定（enabled: デフォルト true / scope: header・full）。既存 data.json は自動補完（マイグレーション不要） |
| `setupAutoReadTTS` | realclaudian view の `onTabStreamingChanged` をチェーンし、ストリーミング完了（true→false）を検出。hook ポイント消失時は Notice 警告で graceful 退化 |
| `extractReportText` | 最後のアシスタントメッセージから 📢 blockquote を抽出。`data-cb-tts-read` 属性で二重読み上げを防止 |
| `createLatestWinsSpeaker` | 読み上げの直列化コーディネータ。WebSpeech は synth.cancel で真の中断、edge/Plachta は最新1件のみ保留して順次読み上げ |
| 設定タブ UI | 「📢 タスク終了時の自動読み上げ」セクション（有効トグル + 範囲ドロップダウン）。ja/en/zh 対応 |

---

## 🔧 最適化と改善

| 最適化項目 | 説明 |
|--------|------|
| ユニットテスト追加 | settings 4 件 / extract-report 8 件 / speak-coordinator 4 件 / auto-read 9 件（計 25 件・全 395 件 PASS） |

---

## ⚠️  Breaking Changes

> [!warning] なし
> 既存設定との互換性を維持（autoRead 欠落時はデフォルト ON で補完）。履歴会話の表示では読み上げられない（ストリーミング完了遷移でのみ発火）。

---

## 🔗 関連リンク

- [[バージョン履歴]] — バージョン履歴
- [[既知の問題]] — 既知の不具合
- [[../02_設計文書/2026-08-14-task-completion-auto-tts-design|タスク終了時自動TTS読み上げ設計書]]

---

<details>
<summary>📜 v0.10.0（履歴）</summary>

# 📢 v0.10.0 リリースノート

> 📅 リリース日付: 2026-08-14

## 🎯 バージョンの強み

> [!success] 新機能
> - ✨ ClaudeTTS（voice-config.json）設定融合 — Claudian Bridge が SSOT として CLI 設定と双方向同期
> - ✨ エンジン別チャンキング — Plachta（900字）/ WebSpeech（200字）で長文を自動分割して正常読み上げ
> - ✨ TTS 設定タブに「🖥️ Claude Code CLI 用設定」セクションを追加

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| `tts.cli` | Claude Code CLI 用設定（full_text / max_chars / debounce_ms / speech_filter）。voice-config.json と同期 |
| `VoiceConfigSync` | `~/.claude/skills/claude-tts/voice-config.json` を初回 import、保存時に export。Claudian Bridge を SSOT とする |
| `chunkText` / `speakChunks` | エンジン別文字数制限対応の長文チャンキング（句読点境界で max までパッキング） |

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-002 | WebSpeech API が `onend` を待たず 100ms で成功判定し、長文が途中で途切れてもエラーにならない問題 | ✅ 修正（onend で解決 + 30 秒タイムアウトガード） |
| KB-003 | Plachta 1000 字超で「エラー表示」される問題 | ✅ 修正（900 字チャンクで自動分割） |

</details>

<details>
<summary>📜 v0.9.0（履歴）</summary>

# 📢 v0.9.0 リリースノート

> 📅 リリース日付: 2026-08-14

## 🎯 バージョンの強み

> [!success] 新機能
> - ✨ Claudian チャットのコードブロックをコピーするとき、``` のコードフェンスを自動付与
> - ✨ 一般タブに有効/無効トグル（デフォルト有効）を追加

---

## 🆕 新規追加機能

| 機能 | 説明 |
|------|------|
| `general.codeCopyFence` | Claudian チャットのコードブロックコピー時に ``` フェンスを付与。realclaudian がフェンスを欠落させる問題の workaround |

---

## 🐛 不具合修正

| Bug ID | 説明 | 状態 |
|--------|------|------|
| KB-001 | Claudian チャットの Mermaid コードブロックをコピーすると ``` フェンスが欠落し、MD 貼り付け後に描画されない（realclaudian がレンダリング後 DOM の textContent をコピーするため） | ✅ workaround 実装（本番は上流修正待ち） |

</details>

</details>

---

*📢 v0.19.0 リリースノート · MiuMiu 🐾*

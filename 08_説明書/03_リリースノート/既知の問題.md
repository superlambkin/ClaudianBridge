---
title: "既知の問題"
type: known-issues
template_id: poc/v3.1.0/08-既知の問題
template_version: 3.5.0
version: 2.1.0
status: stable
created: 2026-06-21
modified: 2026-09-15
tags: 
  - 既知の問題
  - known-issues
  - bug
  - v0.49.1
aliases: 
  - 既知の問題
language: Japanese
applied_rules_version: 2.15.0
---

> 📂 **パス**: `08_説明書/03_リリースノート/既知の問題.md`
> 🎯 **用途**: 現在のバージョン（**v0.49.1**）の既知の不具合一覧

---

# 🐛 既知の問題

> [!info] **v0.49.1 時点の状況**
> v0.49.1 ホットフィックスで **🔴 高優先度の Forced reflow 嵐（KB-022/023）を解消**しました。
> その他の解決済み問題は「✅ 解決済み」セクションを参照してください。
> 残り reflow 派生問題は次セッションで Performance 計測 → 根本対策を予定。

---

## 🔴 高優先度

**なし**（KB-022/023 を v0.49.1 で解消済み）

---

## 🟠 中優先度

**なし**

---

## 🟡 低優先度

| Bug ID | 問題の説明 | 影響範囲 | 状態 | 修正予定 |
|--------|----------|----------|------|----------|
| KB-001 | realclaudian のコードブロックコピーが ``` フェンスを欠落（Mermaid 貼り付け後描画不可） | Claudian チャット → MD 貼り付け | 🟡 上流対応待ち（claudian-bridge v0.9.0 で workaround 提供） | realclaudian 側修正 |

---

## 📋 問題統計（v0.49.1 時点）

| 状態 | 件数 |
|------|------|
| 🔴 高優先度（未解決） | **0** |
| 🟠 中優先度（未解決） | **0** |
| 🟡 低優先度（未解決） | **1**（realclaudian 上流依存） |
| **合計（未解決）** | **1** |
| ✅ 解決済み（v0.6.0〜v0.49.1） | **23**（KB-001〜KB-023） |

---

## ✅ 解決済み（v0.6.0 〜 v0.49.1）

| Bug ID | 問題の説明 | 解決内容 | バージョン |
|--------|----------|----------|------|
| KB-001 | Mermaid コードブロックコピー時のフェンス欠落（workaround） | realclaudian 側修正待ち（claudian-bridge 側で v0.9.0 workaround 提供） | v0.9.0 |
| KB-002 | WebSpeech が onend を待たず成功判定 | onend 完了を待ってから settled 状態へ遷移 | v0.10.0 |
| KB-003 | Plachta 1000 字超でエラー表示 | テキスト上限内に制限・speed はデフォルト 1.0 にフォールバック | v0.10.0 |
| KB-004 | 自動読み上げ不発火（hook 先の誤り） | `tabManager.callbacks.onTabStreamingChanged` に hook ポイント修正 | v0.11.1 |
| KB-005 | 全文ボタンと Bridge 設定が非同期 | 設定反映を `store.onSave` 経由に統一 | v0.11.1 |
| KB-006 | edge 意図的停止後にエラー Notice | 停止ハンドラの Notice 抑制 | v0.12.0 |
| KB-007 | 旧📖 ON と新ボタン表示の不整合 | 旧キー検出時の自動変換（autoReadReportScript） | v0.12.0 |
| KB-008 | 📢 なし応答で誤報通知 | `extractReportText` の判定強化 | v0.12.1 |
| KB-009 | 複数タブ時 `.claudian-messages` 取得固定 | `view.getTabManager()` 経由で動的取得 | v0.12.1 |
| KB-010 | Claude Code CLI の Stop hook が `~/.claude/hooks/tts-speak.py` を参照するがスクリプトが欠落 | `tts-speak.py` を claude-tts スキルの `hooks/stop_hook.py` を呼ぶラッパーとして復元 | CLI 設定（2026-08-15） |
| KB-011 | speech_filter が読み上げに未適用 | `speakText` 統合経路に `resolveSpeechFilter()` 適用 | v0.12.2 |
| KB-012 | claude-tts スキルの extractor 欠落 | extractor.ts を claude-tts スキルに追加 | v0.12.2 |
| KB-013 | ミュートボタンが点滅しない | `isTtsPlaying` の reactive 通知経路を修正 | v0.12.3 |
| KB-014 | ミュートボタンで音声停止しない | `stopAllPlayback` + `PlaybackController.stop()` 連動 | v0.12.3 |
| KB-015 | ミュート停止が不確実（PID 追跡） | spawn PID を `playback-registry` で一元管理 | v0.12.6 |
| KB-016 | LLM 原稿生成 Notice「📝 原稿生成中 n/m…」残留バグ | `progressHidden` フラグ + `onProgress(X/X)` で冪等 hide | v0.37.2 |
| KB-017 | OpenVPN Windows 版 2.7.x で stdout 監視不足 | `stdout` + `stderr` 両ストリーム監視（接続成功/AUTH_FAILED 検出） | v0.43.3 |
| KB-018 | VPN 接続済み表示なのに通信不能（route 追加失敗の誤判定） | 接続 2.5 秒後に `route print` で VPN 経路検証・警告表示 | v0.44.1 |
| KB-019 | Obsidian クラッシュ時の孤児 openvpn プロセスによる TAP アダプタ占有 | `reapOrphanOpenVpn()` で PID ファイル + auth マーカーの 2 経路で自動回収 | v0.44.0 |
| KB-020 | VPN stop() 内の `setTimeout(...).unref is not a function`（Obsidian ブラウザ環境） | optional 呼び出し `stopTimer.unref?.()` に変更 + ブラウザ相当環境の回帰テスト追加 | v0.44.2 |
| KB-021 | Zhipu Python 依存の沈黙障害（スクリプトパス不一致 + zai-sdk 未導入） | `createZhipuProvider` を Python spawn から `httpGet`（生 Bearer キー）に置換、`_query_zhipu_quota.py` 削除 + zai-sdk アンインストール | v0.48.0 |
| **KB-022** | **chat-read-highlight の Forced reflow 嵐による UI フリーズ**（ピーク 114ms） | **`scrollIntoView` を `requestAnimationFrame` 経由の `behavior:'auto'` 化**。ピーク 114ms → **53ms**（-53.5%） | **v0.49.1** |
| **KB-023** | **md-read-highlight も同じ reflow 問題を抱えていた（同時パッチ）** | **rAF 内に `getBoundingClientRect` を 1 回だけ集約**。ピーク 90ms → **51ms**（-43%） | **v0.49.1** |

---

## 📞 フィードバック窓口

| 窓口 | リンク |
|------|------|
| **GitHub Issues** | https://github.com/superlambkin/ClaudianBridge/issues |
| **GitHub Releases** | https://github.com/superlambkin/ClaudianBridge/releases |

---

## 🔗 関連リンク

- [[80_POC_Projects/POC_017_ClaudianBridge/08_説明書/03_リリースノート/リリースノート|リリースノート（最新）]]
- [[80_POC_Projects/POC_017_ClaudianBridge/08_説明書/03_リリースノート/バージョン履歴|バージョン履歴]]
- [[80_POC_Projects/POC_017_ClaudianBridge/04_テスト文書/04_欠陥追跡|欠陥追跡（KB 全 23 件）]]
- [[80_POC_Projects/POC_017_ClaudianBridge/02_設計文書/32_チャット読上げハイライト設計|F-050 設計]]

---

*🐛 既知の問題 v2.0.0 · Claudian Bridge v0.49.1 同期 · MiuMiu 🐾*

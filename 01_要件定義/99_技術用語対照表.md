---
title: "99_技術用語対照表"
type: glossary
template_id: poc/v3.1.0/99-技術用語対照表
template_version: 3.5.0
version: 2.2.0
status: stable
created: 2026-06-21
modified: 2026-09-15
tags:
  - 用語集
  - glossary
  - 技術用語
  - 日英対照
aliases:
  - 技術用語
  - 用語
  - 用語集
language: Japanese
applied_rules_version: 2.15.0
project_id: POC_017_ClaudianBridge
---

> 📂 **パス**: `80_POC_Projects/POC_017_ClaudianBridge/01_要件定義/99_技術用語対照表.md`
> 🎯 **目的**: Claudian Bridge プロジェクト技術用語の日英対照（As-Built v0.49.1、2026-09-15 同期）

---
# 📖 技術用語対照表

## 🎯 機能/業務用語

| 用語 | 英語 | 略称 | 定義 |
|------|------|------|------|
| Claudian Bridge | Claudian Bridge | CB | 本プラグイン。4 つの旧プラグインを統合した統合ブリッジ（**v0.49.1**） |
| Add to Claudian | Add to Claudian | — | 選択テキスト/フォルダ/UI 要素を realclaudian チャット入力欄に挿入する機能（F001/F003/F009） |
| Add to TTS | Add to TTS | — | 選択テキストを音声読み上げする機能（F002）。MD ファイル右クリック版は F015/F028/F031/F051 |
| オブジェクトコンテキストメニュー | Object Context Menu | OCM | Obsidian UI 要素右クリックで要素情報を `@object[type]` 形式で挿入（F009） |
| 拡張子ホワイトリスト | Extension Whitelist | EW | ファイル一覧を CSS 注入で拡張子フィルタする機能（F005）。`_` フォルダ非表示は F020、`.` 始まりの非表示は F049（`hideDotFolders`） |
| LLM 残量検知 | LLM Quota Detection | Quota | Claude / DeepSeek / Kimi / MiniMax / Zhipu の **5 プロバイダ**残量を信号色で常時表示（F011）。**Zhipu は v0.48.0 で純 TS 化**（F048） |
| プロバイダローテーション | Provider Rotation | — | 残量インジケータの表示プロバイダを `quotaSwitchSec` 周期で切替える機構 |
| 設定自動移行 | Settings Migration | — | 旧 4 プラグインの data.json を新形式へ自動変換（F007） |
| AI 読み上げボタン | AI Read Button | ✨ | 入力欄のテキストを claude -p で整形し既存 TTS で読み上げるボタン（F013） |
| MD 保存ボタン | MD Save Button | — | LLM 回答ブロック / ツールバーから Markdown を保存（F014） |
| speakText 統合 | speakText Unified Entry | — | 全 TTS 経路（selection/autoRead/message/inputAi）を集約するエントリ（F015） |
| 最終回答ゲート | Final Answer Gate | — | マルチターン応答で思考・ツール実行中の誤発火を防ぐ `detectFinalAnswerState()`（F017） |
| Chroma-fs RAG | Chroma-fs RAG | — | chroma_db 仮想フォルダ非表示 + 右クリック RAG 検索（F018） |
| バックアップ | Backup | 💾 | ファイルツリー右クリック → タイムスタンプ付き `fs.promises.cp`（F019） |
| クイック返信方案ボタン | Quick Reply Option Buttons | — | ✅❌1️⃣〜5️⃣ 方案ボタン。動的表示（F021）・全体 ON/OFF（F022）・案検出拡張（F023） |
| 完了報告スクリプト | Report Script | — | 完了報告を ヘッダー/結論/次のアクション提案 に整形して読み上げ（F026） |
| トークン速度 | Token Rate | tok/s | LLM 応答のトークン生成速度を YOLO トグル左に表示（F027） |
| MD 読み上げハイライト | MD Read Highlight | — | MD Preview 上でチャンク単位に下線 + フローティングオーバーレイ（F028）。**v0.49.1 で rAF 化** |
| Mermaid 自動描画 | Mermaid Auto-render | — | チャット mermaid フェンスを Obsidian MarkdownRenderer で自動図化（F030） |
| 再生制御強化 | Playback Control | — | PlaybackController（新設）で ⏸/⏭ 実働化・自然分割・先行変換（F031） |
| 聴き手プロファイル | Listener Profile | — | Add to TTS で原文/職場/顧客/家族/教室/上司/DR を切替（F015 拡張） |
| 選択ポップアップ位置 | Selection Popup Position | — | テキスト選択時のフローティングポップアップ位置 `top-right`（既定）/ `bottom`（F032） |
| LLM 原稿整形 | LLM Script Polishing | — | 入力テキストを claude -p で整形する際のパイプライン。「です・ます調」統一・並列生成（`tts.llmRewriteConcurrency` 1〜8）・ストリーミング読上げ・中断堅牢化（v0.37.1〜v0.37.2、F033） |
| MD クリーン化 | MD Clean-up | — | `<style>` / `<script>` タグを除去してから読み上げ（F038） |
| 🤖 Think モード選択 | Think Mode Selector | — | TTS 経路で LLM を直接呼び出す際の thinking 表示制御。Claude (Phase 1/F039) + DeepSeek/Zhipu/MiniMax/Kimi (Phase 2/F040) |
| 🌐 ネットワークタブ | Network Tab | — | 設定 UI の 🌐 ネットワークタブ。プロキシ設定を一般タブから移動 + OpenVPN 接続セクション新設（F042） |
| OpenVPN 接続 | OpenVPN Connection | — | `.ovpn` ファイル + auth-user-pass での VPN トンネル確立（デスクトップ環境のみ・F041） |
| Server Override | Server Override | — | DDNS ドメインで `.ovpn` の remote を上書き（F044） |
| 残骸経路 | Stale Route | — | 過去セッションの openvpn が残した経路で、現セッションの正しいゲートウェイと一致しないもの。検知 F045 → 手動削除 F046 → 自動削除 F047 の 3 段防御 |
| OpenVPN 孤児プロセス | Orphan OpenVPN Process | — | Obsidian クラッシュ・強制終了時に `onunload.stop()` を経由せず生き残った openvpn.exe。`reapOrphanOpenVpn()` で PID ファイル + auth マーカーから自動回収（F041） |
| Outputs フォルダミラーリング | Outputs Folder Mirroring | — | `Documents/ObsidainOutputs` を Vault/Outputs として NTFS ジャンクションで表示（F049）。`hideDotFolders` と 📜 改定履歴タブも同梱 |
| 💬 チャット読上げハイライト | Chat Read Highlight | — | 自動読上げ中のアシスタントメッセージに amber 背景 + 黄色左罫線（`cb-chat-read-active` クラス・F050）。世代トークンで割り込み時誤解除防止。**v0.49.1 で rAF 化** |
| 📄 MD 画面 Add to TTS ボタン | MD View Add to TTS Button | — | MD Preview のビューヘッダ右上 ✏️ の左に 🔊 ボタン。`MarkdownView.addAction('volume-2', ...)` で描画（F051） |

## ⚙️ 技術用語

| 用語 | 英語 | 略称 | 定義 |
|------|------|------|------|
| realclaudian | realclaudian | — | Claudian 本体プラグイン（統合対象外・稼働前提依存）。内部 API `activateView`/`getView`/`appendToActiveInput`/`newTabButtonEl` を利用 |
| edge-TTS | edge-TTS | edge | Microsoft Edge 系 TTS。v0.24.0 で v7.2.8 を `py/edge_tts/` に git subtree 同梱（pip 依存ゼロ） |
| edge_tts 同梱 | edge_tts Bundled | edge-local | `src/features/tts/` から `py/edge_tts/` を spawn。Linux 対応・SIGTERM→SIGKILL（F024） |
| Web Speech API | Web Speech API | webspeech | ブラウザ標準の音声合成 API。音色はブラウザ依存 |
| Plachta Cloud VITS | Plachta VITS (HF Space) | plachta | Hugging Face Space 上のクラウド VITS 音声合成。9 プリセット（ウマ娘/原神/サノバウィッチ × 日中英） |
| VITS | Variational Inference with adversarial learning for end-to-end TTS | VITS | End-to-End 音声合成モデル |
| markitdown | markitdown | — | Microsoft 製 Office/PDF/HTML/CSV → Markdown 変換 Python ライブラリ（F004） |
| word-pdf-rag | word-pdf-rag | — | Chroma-fs RAG で使う `query.py --json` Python サブプロセス（F018） |
| Claude Code CLI | Claude Code CLI | `claude -p` | stdin 経由で整形プロンプトを渡す外部 CLI。30 秒タイムアウト（F013） |
| Zhipu GLM | Zhipu GLM | Zhipu | F011 の 5 プロバイダ目。非公式 usage API 経由（規約上グレー・R-12 監視中） |
| Chroma | Chroma DB | — | ベクトルデータベース。F010 でブラウズ・検索対象。F018 で RAG 質問 |
| ConfigStore | Config Store | — | `core/config-store.ts`。data.json の load/save/watch/close を担う基盤 |
| normalize / validate | Normalize / Validate | — | 設定読み込み時の正規化（デフォルト補完・クランプ）と検証（`core/settings.ts`） |
| CSS 注入 | CSS Injection | — | 拡張子フィルタ・`_` フォルダ非表示の実現方式。動的生成 CSS を `<style>` として挿入/除去 |
| `_disabled__` リネーム | `_disabled__` rename | — | 旧プラグイン無効化方式。フォルダ名に接頭辞を付与（物理削除しない） |
| 防御的アクセサ | Defensive Accessor | — | realclaudian 内部構造への複数経路フォールバック参照（R-01 対策） |
| 診断ログ | Diagnostic Log | diag | `core/diag.ts` によるロード各ステップ記録（F012）。`debug.mermaid.log` は F030 で追加 |
| HF Space | Hugging Face Space | — | Plachta TTS のホスティング先。スリープ・レート制限あり（R-04） |
| MarkdownRenderer | Obsidian Markdown Renderer | — | F030 で mermaid フェンス描画に使う標準 API（mermaid は非同梱） |
| PlaybackController | Playback Controller | — | F031 で新設。⏸/⏭ が音声本体を実制御。`aborted` フラグで spawn にも停止指示 |
| PlaybackController.aborted | PlaybackController.aborted | — | speakChunks ループ先頭で即 `return false` する明示的な abort フラグ（F031） |
| chunkTextNatural | chunkTextNatural | — | F031 の見出し強制分割チャンク。TTS 本体とハイライト index を完全一致させる |
| scrollPositionPct | scrollPositionPct | — | F031 の自動スクロール位置設定（0〜100・既定 40）。CSS 変数として即時反映 |
| `--cb-md-read-highlight` | CSS 変数 `--cb-md-read-highlight` | — | F028 のハイライト色バインド。SettingTab の hex 入力から即時反映 |
| debug.mermaid.log | debug.mermaid.log | — | F030 の mermaid 描画失敗ログ。プラグイン dir 直下へ追記（diag.ts パターン） |
| tokenRateIntervalMs | tokenRateIntervalMs | — | F027 の更新周期プリセット 100/250/500/1000/2000 ms・既定 250（v0.32.0） |
| tokenRateShowTtft / Current / Avg / Max | tokenRateShowFlags | — | F027 の項目個別表示フラグ（v0.31.0）。既定すべて ON |
| quickReplyShowAllOptions | quickReplyShowAllOptions | — | F021 1️⃣〜5️⃣ 常時表示フラグ（既定 OFF） |
| quickReplyEnabled | quickReplyEnabled | — | F22 クイック返信ボタン全体 ON/OFF フラグ（既定 ON） |
| autoReadReportScript | autoReadReportScript | — | F026 完了報告スクリプト整形 ON/OFF（既定 ON・i18n ja/zh/en） |
| mdReadHighlight | mdReadHighlight | — | F028 の `{ enabled, highlightColor }` 設定オブジェクト |
| mermaidRender | mermaidRender | — | F030 の `general.mermaidRender` 設定（既定 ON） |
| mdReadProfile | mdReadProfile | — | F015 拡張の聴き手プロファイル（`original` / `workplace` / `customer` / `family` / `classroom` / `boss` / `dr`） |
| NTFS ジャンクション | NTFS Junction | — | Windows のリパースポイント機能。`Documents/ObsidainOutputs` を `Vault/Outputs` として透過表示（F049） |
| TAP アダプタ | TAP Adapter | — | OpenVPN が使用する仮想ネットワークアダプタ。Windows では `tap-windows6`。孤児プロセス占有時は新規接続が失敗（F041・R-15） |
| `route print` | route print | — | Windows のルーティングテーブル表示コマンド。VPN 経路検証と残骸検知で使用（F045/F041 副次） |
| `--remote` | --remote | — | openvpn CLI の接続先上書きオプション。`host` または `host:port` で DDNS 上書き（F044） |
| `--writepid` | --writepid | — | openvpn CLI の PID ファイル書き出しオプション。孤児プロセス回収の主経路（F041） |
| `--auth-user-pass` | --auth-user-pass | — | openvpn CLI の認証情報ファイル指定。chmod 600 で権限制限（F041） |
| `[DHCP-serv: x.x.x.x]` | DHCP-serv log line | — | openvpn ログのそのセッションの正しいゲートウェイ記録。経路表との照合に使用（F045） |
| `Initialization Sequence Completed` | Init Completed | — | openvpn の接続成功ログ。route 追加失敗時も出力されるため、F041 副次で経路検証が別途必要（F044.1） |
| raw Bearer キー | Raw Bearer Key | — | API キーを JWT 等に変換せずそのまま `Authorization: Bearer <key>` で送る方式。Zhipu 純 TS 化で採用（F048） |
| `httpGet` | httpGet | — | プラグイン内の統一 HTTP GET ヘルパー。DeepSeek / Kimi / MiniMax / Zhipu / MiniMax が共通利用 |
| `open.bigmodel.cn/api/monitor/usage/quota/limit` | Zhipu Quota Endpoint | — | Zhipu（智譜 GLM）の残量取得エンドポイント。**生 Bearer キー**で 200 OK（F048） |
| unit 3 / unit 6 | Quota Unit | — | Zhipu の残量単位。`unit 3` = 5時間窓、`unit 6` = 週窓。`window='5h'` で unit 3、無ければ unit 6 にフォールバック（F048） |
| chatReadHighlight | chatReadHighlight | — | F050 の `{ enabled: boolean }` 設定オブジェクト（既定 ON）。tts 名前空間に所属 |
| `cb-chat-read-active` | cb-chat-read-active | — | F050 のハイライト用 CSS クラス。`.claudian-message-assistant.cb-chat-read-active` で対象指定 |
| `requestAnimationFrame` | rAF | — | ブラウザの次フレーム描画前コールバック。`scrollIntoView` の同期 layout thrashing を断つために使用（**v0.49.1 ホットフィックス**） |
| Forced reflow | Forced Reflow | — | JavaScript がレイアウトスラスタを強制する状態。Forced reflow 警告（Chromium）で通知される（v0.49.1 でピーク 114ms → 53ms に削減） |
| generation トークン | Generation Token | — | 世代番号でハイライトの activate/deactivate を識別する機構。割り込み時に旧 speak finally が新ハイライトを誤解除しない（F050） |
| LlmClient 抽象化 | LlmClient Abstraction | — | Claude / DeepSeek / Zhipu / MiniMax / Kimi の 5 プロバイダを統一インターフェースで扱う抽象層。`dispatch.ts` で選択（F039/F040） |
| `reapOrphanOpenVpn()` | reapOrphanOpenVpn() | — | 前回セッションの openvpn 孤児プロセスを自動回収する関数。`--writepid` PID ファイル + auth マーカーの 2 経路で検出（F041） |
| `OpenVpnController.getWarning()` | getWarning() | — | ネットワークタブに表示する VPN 警告文字列を返す API。経路未確立・残骸検知などで使用（F041 副次/F045） |
| `network.openvpn.*` | network.openvpn | — | v0.43.0 で新設された OpenVPN 設定名前空間（`configPath` / `autoConnectOnLlm` / `serverOverride` など） |
| `network.proxy.*` | network.proxy | — | v0.43.0 で一般タブから移動したプロキシ設定（`general.proxy` → `network.proxy`、旧キーは normalize 時に自動移送） |
| `general.outputsMirrorEnabled` | outputsMirrorEnabled | — | Outputs フォルダミラーリングの有効化フラグ（既定 OFF・F049） |
| `general.hideDotFolders` | hideDotFolders | — | `.` 始まりのフォルダ非表示フラグ（既定 ON・F049） |
| `general.outputsMirrorPath` | outputsMirrorPath | — | ミラーリング元パス（既定 Documents/ObsidainOutputs 動的解決・F049） |
| `tts.llmRewriteConcurrency` | llmRewriteConcurrency | — | LLM 原稿の並列生成数（1〜8・既定 2・F033） |
| `selection.popupPosition` | popupPosition | — | テキスト選択時のフローティングポップアップ位置（`top-right` 既定 / `bottom`・F032） |
| `tts.thinkMode` | thinkMode | — | TTS 経路での LLM 直接呼び出し時の thinking 表示モード（`enabled` / `disabled` / `auto`・F039/F040） |
| `MarkdownView.addAction` | MarkdownView.addAction | — | Obsidian 公開 API。MD 画面ビューヘッダにアイコンを追加（F051） |
| `check-changelog.mjs` | check-changelog | — | リリース時に CHANGELOG 更新を強制する npm script。`npm run check:changelog`（F049） |

## 🏗️ プロジェクト管理用語

| 用語 | 英語 | 略称 | 定義 |
|------|------|------|------|
| As-Built 要件 | As-Built Requirements | — | 計画ではなく実装済み実績を正とする要件記述方式（本要件定義 v2.0.0 の方針） |
| P2〜P5 | Phase 2〜5 | — | 開発フェーズ：P2 コア移行 / P3 Office 統合 / P4 拡張子フィルタ / P5 拡張機能 |
| UAT | User Acceptance Test | UAT | 手動受け入れテスト。全 AC は UAT で検証済み |

## 📊 テスト用語

| 用語 | 英語 | 略称 | 定義 |
|------|------|------|------|
| vitest | Vitest | — | ユニットテストランナー。`tests/` は `src/` とミラー構成、obsidian はスタブ化 |
| Given-When-Then | Given-When-Then | GWT | AC 記述形式（[[80_POC_Projects/POC_017_ClaudianBridge/01_要件定義/06_合格基準_AC]] 参照） |
| トレーサビリティマトリックス | Traceability Matrix | RTM | F 番号 ↔ AC ↔ テストの対応表 |

---

## 🔗 関連リンク

- [[80_POC_Projects/POC_017_ClaudianBridge/01_要件定義/00_要件総覧]] — 要件総覧
- [[../02_設計文書/00_アーキテクチャ総覧]] — アーキテクチャ総覧

---

## 📝 更新履歴

| バージョン | 日付 | 修正内容 | 修正者 |
|------|------|---------|--------|
| v1.0.0 | 2026-06-21 | 初版：用語集テンプレート | MiuMiu 🐾 |
| v2.0.0 | 2026-08-13 | As-Built 全面改訂：実用語 25 件を定義（機能 8 / 技術 13 / 管理 3 / テスト 3） | MiuMiu 🐾 |
| v2.1.0 | 2026-09-05 | As-Built v0.35.1 同期：機能用語 14 件（F013-F031 関連）+ 技術用語 16 件（edge_tts 同梱 / word-pdf-rag / Claude Code CLI / Zhipu / MarkdownRenderer / PlaybackController / scrollPositionPct / mdReadProfile / 各種設定キー）を追加。総計 60+ 件 | MiuMiu 🐾 |
| v2.2.0 | 2026-09-15 | As-Built v0.49.1 同期：機能用語 **15 件**（F032/F033/F038/F039-F040/F041-F044/F045-F047/F048/F049/F050/F051 関連）+ 技術用語 **29 件**（NTFS ジャンクション / TAP アダプタ / `route print` / `--remote` / `--writepid` / `--auth-user-pass` / `[DHCP-serv: x.x.x.x]` / `Initialization Sequence Completed` / raw Bearer キー / `httpGet` / Zhipu エンドポイント / unit 3・6 / chatReadHighlight / `cb-chat-read-active` / rAF / Forced reflow / generation トークン / LlmClient 抽象化 / `reapOrphanOpenVpn()` / `getWarning()` / `network.openvpn.*` / `network.proxy.*` / `outputsMirrorEnabled` / `hideDotFolders` / `outputsMirrorPath` / `llmRewriteConcurrency` / `popupPosition` / `thinkMode` / `MarkdownView.addAction` / `check-changelog.mjs`）を追加。総計 **104+ 件** | MiuMiu 🐾 |

---

*📖 技術用語対照表 v2.2.0 · 日英対照 · MiuMiu 🐾 · 2026-09-15 As-Built v0.49.1 対応*
